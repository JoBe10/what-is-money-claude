> **Executed and superseded — archived, not authoritative.** Its standing rules live in `docs/what-is-money-master.md` (Stage 0, 25 August 2026).
> Kept as history. Nothing here governs.

# Rebuild Session Brief — R7.4 (Section 4 Resolution)
## *What Is Money? — And How Does Bitcoin Fit In?*

**Status:** Approved for implementation · **Mode: FAST** (verify only what changes: build clean, changed slides' builds, direct entry + reduced motion for those slides, screenshots of changed states; no deck-wide suites — those run at the next GATE)
**Branch:** continue on `rebuild-r7-1-harmonization`.
**Presenter rulings in force (logged as freeze/protected-list amendments where applicable):** display-scale assets are dark-field everywhere in Section 4; "The framework is explicit. The scores are judgments." removed from 4.15's visible copy; falsifiability relocates from slide to script; the corner-frame motif is retired; the table reveals all scores in one advance.
**Date:** 14 August 2026

---

# A. Session-mode change (first commit)

Add to `AGENTS.md`: two session modes. **FAST** (default for iteration): scoped verification as above, target well under an hour. **GATE** (before any merge, and at R5/R6): the full suite — traversals, matrices, gates, frame captures, regression. A change is not "unverified" in FAST mode; it is verified locally and guaranteed globally at the next GATE.

# B. Register correction — dark-field assets at every display-scale appearance

The two-register rule stands; its application was too conservative. **Every display-scale asset appearance in Section 4 becomes a dark-field render:** the five-candidate lineup (4.15), the migration slide, the other-assets/monetary-premium slides, the marginal-decision slide, and the carrier-candidates lineup. Restore the old renders from git history (gold bar, fiat note, bitcoin coin, property, share certificates, shells, ledger); grade-gate; regenerate any that fail the grade or are missing, via the manifest process. Grammar glyphs remain only at genuine diagram scale: the comparison table's headers and the rail. **Table-header exception test:** implement a toggle rendering the table headers as small dark-field renders vs. glyphs; screenshot both; presenter rules.

# C. The carrier shell — decisive round

The arc shell is retired on sight. Produce a contact sheet of **five genuinely different carrier treatments**, each shown enclosing the LuminousDisc at three scales and in one motion still (the save/carry state):
1. A refined **capsule-derived shell** — the old capsule's confident hull, reinterpreted as an enclosure with the disc glowing inside (dual-stroke, subtle fill, real weight);
2. A **faceted vault ring** (full enclosure, hexagonal/octagonal, material stroke + glow);
3. A **soft vessel** (continuous rounded enclosure, low-opacity warm fill);
4. A **minimal orbit** (single fine full ring, no gaps — the quietest option);
5. **No shell** — the disc alone, carrier identity carried by label/context only.
Fix the 100-YEARS overlap (enforce clearance between shell/disc and any text at every build). **Stop at the contact sheet; the presenter selects.** Apply nothing until selection.

# D. Deletions and simplifications

1. **The corner-frame motif** on "How could the carrier fail?" is deleted (unexplained decoration; fails the legibility-of-meaning rule).
2. **4.15 copy:** remove "The framework is explicit. The scores are judgments." (the spoken layer keeps the scores-are-judgments point at the table; visible copy drops it).
3. **The comparison table:** all fifty scores reveal on a single advance after the table frame lands (replacing the 5+5 split); script `[→]` re-mapped.
4. **The falsifiability slide is deleted.** Its content is preserved verbatim as the closing `[→]` passages of the preceding slide's script (the three conditions + "don't wait for my updated slides — that's what Don't trust. Verify. means when it's meant"), and the Q&A arsenal block moves with it. On-screen, the deck's second "Don't trust. Verify." is retired; the line now appears once, at the table. Freeze register updated.

# E. The stability slide — rebuilt around one idea, or nothing

Replace the current text-wall with a single visual argument. **Build 0:** a jagged, live price line draws across the upper stage — label, small: THE PRICE. **Build 1:** beneath it, a dead-flat line draws — label: THE RULES. The contrast is the entire visual. **Build 2:** display statement (display rule typography): **"Bitcoin does not fix its price."** / **"It fixes the rules through which the market discovers its price."** — second line in accent. Nothing else on the slide; the two-question framework, volatility-as-stage, and expectation-not-guarantee material live entirely in the script (steelman content preserved word-for-word in notes). If the presenter judges the rendered slide short of the bar, the fallback — pre-authorized — is deletion to script, like falsifiability.

# F. Carried-over fixes (from the prior review, still pending or regressed)

1. **The surgeon photograph:** verify the R7.2-specified restoration actually renders (the current build shows a pulse glyph on black — either a regression or the phase never landed); the photo returns, dark-field graded, on the density-restaged slide.
2. **The steak render:** appears as a meal glyph on the unfinished-exchange and spend-or-save slides while shoe and wine render correctly — fix the reference/gate failure so all three goods are renders.
3. **Migration slide geometry:** each claim disc centers exactly on its vertical connector; connectors meet the baseline cleanly.
4. **Self-reference sweep:** no visible line or script names the medium (presentation/deck/slides/video). The layered-money final line becomes **"The real question is about the foundation asset — underneath them all."** Sweep and rewrite every other instance in the inquiry voice; list each change in the report.
5. **Display rule application:** the definition slide restages to display treatment ("AN EARNED, TRANSFERABLE / CLAIM ON VALUE" stacked at display scale with the disc — not a caption line); audit Section 4 for other single-bright-statements shrunk to captions and promote them, per the display rule.
6. **Settle-state budgets:** the Bitcoin-placement and palladium slides settle prior lines small-and-receded when their final statements land (final-frame budget: one bright statement + labels).

# G. Verification (FAST)

Build clean; changed slides only: every build renders correctly, direct entry + reduced motion pass, before/after screenshots per change under `review/rebuild-r7-4/`; the §C and §B-toggle contact sheets delivered for presenter selection; report section appended with the self-reference sweep list and any judgment calls. Stop without merging.
