> **Executed and superseded — archived, not authoritative.** Its standing rules live in `docs/what-is-money-master.md` (Stage 0, 25 August 2026).
> Kept as history. Nothing here governs.

# Rebuild Session Brief — R7.1 (Section 4 Visual Harmonization)
## *What Is Money? — And How Does Bitcoin Fit In?*

**Status:** Approved for implementation
**Scope:** Bring Section 4's entire visual execution — imagery, composition, text density, headers, the claim/carrier skin — onto the deck's compositional standard, codified below. **The frozen argument, all scripts installed by R7, all choreography concepts, and all fifty scores are untouched.** This is a re-skin and re-composition, not a redesign.
**Branch:** merge `rebuild-r7-section-4` into `main` first (no-ff, `Merge R7: Section 4 execution rebuild`; both flagged judgment calls are presenter-confirmed as implemented). Then branch `rebuild-r7-1-harmonization`.
**Model guidance:** maximum available effort. Candidates + contact sheets on every new render; presenter override preserved.
**Date:** 31 July 2026

---

# 0. Reading order

This brief; `docs/sections-1-3-rebuild-brief.md`; `docs/section-4-master-document-v2.md`; `docs/icon-grammar.md`; `docs/r7-report.md`; `AGENTS.md`. Then study Sections 1–3 slide by slide — they are the standard being codified — and every Section 4 slide as shipped by R7.

---

# 1. Phase A — Codify the compositional standard

Add to `docs/sections-1-3-rebuild-brief.md` as new §9.4, verbatim:

> ## 9.4 The compositional standard (deck-wide)
>
> What makes this deck's frames read as one hand:
>
> 1. **One idea per frame.** Each build lands at most one new text element. On-screen text is the distillation — a line, not a paragraph; the script carries everything else. When a new line lands, prior lines dim or clear unless they are being deliberately accumulated (a list being built is one accumulating element).
> 2. **The two-element budget.** Outside structural diagrams, at most two text elements are fully bright at once (e.g., a kicker-scale label plus one statement). Sparse labels on diagrams don't count against the budget; paragraphs on slides are forbidden.
> 3. **Line-language visuals.** Nodes, thin strokes, dot terminals, grammar glyphs, luminous discs, canvas fields. No photography, no AI-generated imagery, no filled panels except structural slabs (the tower), no icon-library stock.
> 4. **Negative space is a material.** Centered or mirror-symmetric compositions; generous emptiness; nothing touches frame edges.
> 5. **Quiet chrome.** No persistent per-slide header convention. Kickers are rare, small, and earned (an attribution, a frame that must be named). The deck's structure is carried by the waypoint device, not by headers.
> 6. **Orange is meaning.** The accent marks argument — ignitions, foundations, the claim, emphasis dots — never decoration, never chrome.
> 7. **Black is a beat, not a seam.** (Restated from §3.5.)
> 8. **Structural diagrams are exempt from the text budget but not from the hand:** the comparison table, the rail, the tower, the ladder follow their own established patterns — thin strokes, grammar glyphs, consistent rhythm.

## A2. Retire the Section 4 header convention

Remove the persistent orange-line + kicker header from every Section 4 slide. Retain quiet kickers (S1–3 treatment: small, tracked, low-warmth, no rule line) only where a frame must be named — expected survivors: THE COMPARISON (the table), WHAT WOULD CHANGE THESE SCORES, and the attribution-class kickers. Every other header is deleted; slide identity is carried by content and the deck's structure.

---

# 2. Phase B — The claim/carrier re-skin (presenter-ruled; concept frozen, skin changes)

1. **The ClaimObject becomes the deck's luminous disc** — the exact object from `02-the-conversion` (Section 1): same radial gradient, glow token, and proportions, instantiated at whatever scales the choreography needs. One component or shared render path; the 1.2 token and every Section 4 claim are now literally the same object. (This is the deck's central rhyme made visible: the hours became the disc; the disc is the claim; 4.04's "that's what you've been holding all along" happens on screen.)
2. **The CarrierShell becomes a thin-stroke grammar shell**: a minimal open-form outline (single stroke weight from the grammar, subtle orange stroke — orange as meaning: the carrier is the thing under judgment) enclosing the disc with clear negative space between shell and claim. No filled capsule, no bevels, no gradient body. All existing choreography (enclosure, transport, the spend/save split, dilution, the Open Claim Circuit sequence) is preserved motion-for-motion on the new skins.
3. Reduced-motion and direct-entry parity re-verified for every claim/carrier build.

---

# 3. Phase C — Flagship recompositions [specs binding]

## C1. The surgeon derivation (slides 03–05) — retold in the deck's line language

Delete the surgeon photograph and the sneaker/steak/wine photographs. The story becomes a **node-scene**, deliberately rhyming with Section 2's exchange triangle (same node marks, label treatment, edge language, outside-rule label placement):

- **03 (simple exchange):** two nodes — SURGEON and PATIENT — the delivered-value mark (a small grammar glyph for the operation) crossing the edge between them; the capability labels land per the pacing rule, one per advance, as quiet labels beneath the surgeon node.
- **04 (unfinished exchange):** the return leg: the luminous disc (the claim — §2 skin) travels back along the edge to the surgeon. The **$400 numeral stays** (orange, display scale — it is argument, not chrome). The wanted-goods become grammar glyphs (shoe, meal, wine — Icon Studio extension, three candidates each), arrayed dim at the frame's right, *not yet reachable* — the open half of the exchange. Builds match the installed script's `[→]` count exactly.
- **05 (spend or save):** the existing conceptual choreography on the new skins: SPEND closes the circuit (disc crosses to a goods glyph and both resolve); SAVE holds the disc enclosed in the carrier shell, persisting. Recomposed to the standard (centered, two-element budget).

## C2. The stability frame (4.20 final builds) — the density exemplar

As shipped it shows six simultaneous text blocks. Restage to the standard, same words, sequential builds: **B(n):** STABILITY alone (large, centered). **B(n+1):** the two column headers land as a mirrored pair (one advance — one idea: the split itself), their sub-lines at label scale. **B(n+2):** the columns dim; *"Architecture protects the integrity of the claim. The market decides what the claim can buy."* lands alone. **B(n+3):** it dims; the pair *"Bitcoin does not fix its price."* / *"It fixes the rules through which the market discovers its price."* lands as the frame's final state — the only orange text on the slide. Script `[→]` markers re-mapped accordingly.

## C3. The monetary premium (slide 18)

Same treatment: the equation assembles term by term (one advance each, ASSET VALUE = UTILITY + MONETARY PREMIUM, premium in orange); the three asset tiles become plain glyph + label + sub-label groups (no boxed tiles — the tile borders are filled-panel chrome), landing one per advance; the closing pair lands alone with everything above dimmed. Apply the identical de-boxing to any other slide using tile/panel borders.

## C4. The margin and case frames

The five-candidate array (21) inherits the §2 skins and the S1–3 node treatment (it already uses grammar glyphs from R7 — verify spacing/rhythm against the standard); 4.22 (UnitField) and 4.23 (frozen frame) are expected to already comply — verify, touch nothing on 4.23.

---

# 4. Phase D — The standard sweep

Walk **every remaining Section 4 slide** against §9.4 and bring it into compliance: header removal (A2), text-density restaging (one idea per frame; re-map `[→]` markers wherever builds change; pacing tables updated), de-boxing, photography/stock removal (any remaining instance), type-scale alignment to S1–3, composition centering. For each slide, record in the report a one-line decision log entry: what changed, or "already compliant." Structural diagrams (the table, ten-properties, failure grids) keep their patterns but take the header/chrome and glyph checks. Where a restaging judgment is genuinely open, choose, implement, and flag it for presenter review — do not skip.

---

# 5. Phase E — Verification

1. Merge precondition executed; build clean on `main` before branching (record hash).
2. Full deck traversal forward/back/forward; frame captures at every changed boundary; zero incidental black; reduced-motion matrix over all changed builds; direct entry + mid-animation refresh on every restaged slide.
3. Pacing tables regenerated for all of Section 4; `[→]` = builds everywhere.
4. Gates: frozen elements byte-check (`comparisonRows` unchanged); banned terms; "Don't trust. Verify." exactly twice; no `<img>`/photographic asset references remain anywhere in Sections 1–4 except `docs/`/archive (grep by extension and by asset manifest); no boxed-panel styles outside the tower slabs; header-convention selectors removed.
5. Contact sheets for all new renders (goods glyphs, operation glyph, carrier shell states, disc scale set) under `review/rebuild-r7-1/icon-studio/`; candidates kept.
6. Screenshots: every build of every changed slide; the disc side-by-side with 1.2's token at matched scale (the rhyme check); the de-headered table; the stability restage sequence. Save under `review/rebuild-r7-1/screenshots/`.
7. Docs sync: §9.4 committed (A1); `docs/section-4-master-document-v2.md` §1/§3 updated (harmonization implemented; claim-disc unification recorded as the presenter-approved skin change to the protected system); report `docs/r7-1-report.md` with the per-slide decision log and flagged judgments.

Stop after the report. Do not merge.
