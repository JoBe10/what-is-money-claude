# Act III — r2 Fixes
## Five presenter rulings on the live act: the clean handoff, Argentina's objects, the ladder's pacing, the coffee folded in, the network formed once

**Mode: FAST — lean scope:** checks not listed are not run.
**Model: Opus 5, max effort. Fresh CLI session.** (Ruled staging and fixes — no open design.)
**Branch:** `film-rebuild`. **Prior tag:** `batch-c`. **Tag on completion:** `batch-c-r2`.
**First step:** commit this brief as `docs/batch-c-r2-brief.md`.
**Read before working:** the batch-c report, the live S12–S14 modules and their approved cells, the S9 network gesture (`src/scenes/act-2/`), the band law, the installed S13/S14 scripts.

---

# 1. The presenter's rulings (2 September 2026 — record first, granular commits)

1. **S12 b1 — the clean handoff:** the triad completes its recede **before** the column heads land; the columns arrive on clean black. No element of the triad remains under PRICED IN / PAID IN / SAVED IN. Beat and words unchanged.
2. **S12 — the columns get their objects:** the presenter-generated `usd.png` and `ars.png` gate and ingest (recognizability is ruled content for these two — record the exception to the anonymous-note principle); renders land at the band scale above the codes: USD over the priced-in column, ARS over the paid-in column, and USD **plus the library's real-estate render** over the saved-in column (locate it in the register; if no such render exists, flag in plain English and stub — the presenter will generate). The legacy brick glyph retires to file, superseded by the render.
3. **S13 — the reveal splits:** one `[→]` inserted in the installed script's gate paragraph (zero word changes): MEDIUM OF EXCHANGE lands with "Nobody accepts as payment what they don't expect to hold value."; then UNIT OF ACCOUNT lands with "Nobody writes contracts in what nobody accepts." **S13 = 7 beats**; the map amended; downstream states renumbered.
4. **S14 folds into the ladder world — the standalone objection frame retires.** The scene keeps its four spoken beats, staged continuously on the S13 ladder: b1 — the coffee cup arrives at the MEDIUM berth (its established perch scale) as the objection lands as the statement line; b2 — the placement line beneath the ladder as approved; b3 — the coin lands at stage two, alone, with its line; b4 — the pivot to the whole triad with STORE at full voice, per the approved PORT cell. The S13→S14 boundary is a pure morph — one world. The display-scale cup frame retires to file (the render stays in the library).
5. **S9's network forms exactly once (defect fix):** the overlay entry must never pre-show the completed mesh. The entry state is the hub with its spokes; the formation gesture plays across its advance; the formed mesh persists as the settled state; backward traversal reconstructs states without replaying the formation as a flash. Reduced-motion parity: the settled states reached instantly.

# 2. The work

Re-render the affected cells through the states pipeline (S12 b1–b4 with the clean stage and the objects; S13's split cells; S14's folded cells) into their sheets in place, `states.json` and the flipbooks updated; then rewire the live scene modules to the updated states — the S13 beat split, the S14 fold with its morph boundary, the S12 staging, the S9 gesture fix — every settled state proven at its cell at zero pixels; untouched states byte-identical. Splice positions unchanged (S14's fold changes no slide count — the scene remains, its world merges).

# 3. Out of scope — hard

No design, no other scenes, no Act I/IV work, no script wording changes beyond the one ruled `[→]` insertion, no legacy deletion.

# 4. Verification (the complete list — nothing more)

Gate evidence for the two renders; per-cell checks on re-rendered cells; landed-state proofs on the rewired scenes (S12, S13, S14, S9's affected beats); byte-identity on untouched states; act traversal S11→S15 and the S8→S10 span both ways; reduced-motion parity on changed scenes; `npm run build` clean; boot smoke; commit granularity; clean tree; tag; report per AGENTS.md §16, mode named, **review protocol in plain English: the presenter re-walks Scenes 9 and 11–15 in the deck — the network forming once, the clean handoff, the columns carrying their objects, the ladder revealing stage by stage, the objection living and dying on the ladder — and returns the word that closes the batch.**
