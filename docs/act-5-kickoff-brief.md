# Final Fixes (Acts III–IV) · Act V Foundation and States
## The last three fixes on the live acts, then the film's ending comes home — two sessions

**Mode: FAST, both sessions — lean scope.**
**Model: Opus 5, max effort, both. Fresh CLI session each.**
**Branch:** `film-rebuild`. **Prior tag:** `batch-d`. **Tags:** `acts-3-4-final` after Session 1's Part A; `act-5-foundation` at Session 1's close; `act-5-states` after Session 2.
**First step (Session 1):** commit this brief as `docs/act-5-kickoff-brief.md`.
**Read before working:** the batch-d report (its open items), the live S12, S15, S23 modules and cells, `docs/synthesis-architecture.md` (Act V, Scenes 24–30, and the close), the seven remaining `section-4` Act V sources and `section-5-close/` in full (Act V's script and treatment authority), the master's protected lines (the rules line, the "does not need to replace everything" line) and freeze register, the P1 hours-field implementation (the UnitField's bookend rhyme), the precedent kickoffs.

---

# SESSION 1 — PART A: the three fixes and two leftovers (presenter-ruled, 3 September 2026)

1. **S12 — the household frame retires.** The household sentence is spoken over beat 1 as the empty columns land; the text-only b2 frame retires to file. **S12 = 3 beats** (columns · Argentina's objects · the principle); one `[→]` removed, zero word changes; map, cells, and scene amended.
2. **S23 — the table's headers become renders.** The five candidate glyphs retire; the renders ride as a header band above the table at lineup scale per the rails law — gold, the fiat note, the coin, the house, and shares — with the drawn grammar beneath untouched. Locate a shares render in the register; if absent, use the presenter's `shares.png` if present, else stub and flag. The monetary/productive group labels and the kicker stay as approved; the fifty-score landing is unchanged.
3. **The act's exit lands on the tower (structural ruling, recorded).** The relocated pivot beat keeps its spoken coda verbatim but its visual changes: no return to the triad. On the final advance, the tower recedes to its glowing base slab and **"What makes something a good store of value?"** lands beneath it — the wording is protected as Act IV's hinge question (recorded rationale: Act IV's candidates include real estate and shares, which are store-of-value candidates, not base money; the question must be the one the test answers). The triad-return cell (`s15-b7`) retires to file; the new cell renders; S15 stays 7 beats; the S15→S16 boundary morphs from the base slab into the homecoming.
4. **Leftovers:** the survivors `3-00` and `3-04` are **retired** from the manifest (waypoints were retired by the freeze; `3-04` has no scene) — legacy files on disk, counts stated, boundaries proven; the Scene 16 join keeps the legacy "And."
Proofs on every changed state; tag `acts-3-4-final`.

# SESSION 1 — PART B: Act V's foundation (tag `act-5-foundation`)

Act V (Scenes 24–30) is the case and the close — frozen scripts, the film's landing. Produce the foundation as the learned pipeline requires:

1. **`docs/act-5-provenance.md`** — every frame of Scenes 24–30 classified against the legacy Act V sources with named treatments; **§0 answers the staging question first, per the architecture: descending complexity to silence** — the migration and the monetary premium in the act's abstract register; the savings-vehicle renders at display scale where the architecture names them; the marginal decision asked calmly; **the UnitField as the hours-field's visual rhyme — the film's bookend** (its geometry, density, and timing derived from P1's implemented field, recorded as the rhyme's source); the case from first principles on a full clear; the close as final pure typography over silence. ARGUABLE rows in plain English, one line per side, default stated.
2. **The scripts installed verbatim** into `docs/batch-e-package.md` — the frozen Act V scripts extracted from the legacy sources with the mechanical adaptation ledger only (self-references; the architecture's named distributions — the stability content's Scene 29 share, the diversification and does-not-replace-everything concessions merged where the architecture merges them; the falsifiability passage at its ruled home); `[→]` maps derived from the legacy advance structure. **Protected lines verified character-for-character:** the rules line, the "only needs to become the preferred place to store the next unit of value" line, the closing callback and covenant. **The self-reference ban checked hardest here:** the close names no medium, no sequel, no waypoint. Zero creative drafting; unbridgeable seams flagged in plain English.
3. Report with ARGUABLE rows first, plain English, short sentences; commit granularity; both tags cut.

# SESSION 2 — the Act V beat-state sheet (tag `act-5-states`)

Render every Act V settled state per the installed maps into `review/act-5/states/` (flipbook + `states.json`) by provenance class — PORT verbatim (`approved-port`), ADAPT with its one change (`pending-review`), ARGUABLE rows per the presenter's answers in the prompt (defaults flagged if absent). **The UnitField cells are rendered from P1's field implementation** so the rhyme is real, not resembled; the close's typography cells at the film's final register. No candidates unless the map names NEW; one honest render plus a plain-English flag for anything underdetermined. Lean verification as established; report with **the flipbook protocol: the presenter walks the film's ending as stills — the migration, the premium, the other assets, the marginal decision, the field returning, the case, the silence — and returns his verdicts and the go-ahead that unlocks the final implementation batch.**

# Out of scope (both sessions)

No Act V implementation, no motion, no design, no changes to Acts I–IV beyond Part A's named fixes, no touching protected lines beyond verification.
