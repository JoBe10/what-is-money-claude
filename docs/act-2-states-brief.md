# Act II — Rulings, Scripts, and the Beat-State Sheet
## The provenance map ruled · the scripts installed · every Act II state rendered, with review burden only where design is live

**Mode: FAST.** Records, script installation, and one sheet — one construction task.
**Model: Fable 5, top effort. Fresh CLI session.**
**Branch:** `film-rebuild`. **Prior tag:** `batch-a-r3`. **Tag on completion:** `act-2-states`.
**First step:** commit this brief as `docs/act-2-states-brief.md`, and verify `docs/batch-b-scripts-draft.md` is present in the repo (the presenter placed it) — if absent, stop and flag.
**Standing law applies in full:** the provenance rule (AGENTS.md §4 — verify every frame's class before touching it), the full-coverage rule, the aesthetic law, the no-invention rule, the session-sizing law.
**Read before working:** `docs/act-2-provenance.md` (the ruled map after §1 below), `docs/batch-b-scripts-draft.md`, `docs/batch-b-package.md`, the legacy sources the map names, the act-2 systems sheet (reference for confirmed-NEW frames only).

---

# 1. The presenter's rulings (31 August 2026 — record first, each its own commit)

1. **The four ARGUABLE rows:** S5-F1 = **PORT** (`4-06`'s ClaimObject-in-CarrierShell, construction C — with the presenter's carrier-centering ruling already in the component; the photographic renders remain the historical objects in the same scenes). S5-F3 = **ADAPT** (`2-07`'s dated-fact treatment, one ruled change: *generalized as the film-wide evidence grammar* — Zanzibar, 1971, and 1803 all speak it). S7-F2 = **NEW** (the legacy detaching-rung's meaning is vertical abstraction; the film's moment is outward travel with a dependency line back). S8-F2 = **PORT** (the legacy chart untouched, frozen data and all; an ADAPT is available later only if the presenter's in-context viewing rules a clash).
2. **The §0 convention:** per the presenter's answer in the session prompt. Apply it to the rows it governs and record it in the map. If the bracket is empty, flag those rows BLOCKED and continue with everything else.
3. **Palladium stays** (presenter, 30 August): compressed into Scene 10 per the freeze ruling — recorded so the question doesn't reopen.
4. **Standing Act III markup** (presenter, 30 August), recorded now for the future map: the monetization-stages timeline is pre-classified **ADAPT** — ruled change: *basic icons replaced by dark-field renders at lineup scale, staged as an object sequence* (register boundary law respected: renders never inside a diagram). No Act III work in this session.
5. **The Act II scripts are approved as drafted.** Install `docs/batch-b-scripts-draft.md` §-by-§ into `docs/batch-b-package.md` §2 as the verbatim scripts (mechanically diffed against the draft — zero word differences), and freeze the beat maps to the scripts' advance counts: **S5 = 8, S6 = 5, S7 = 5, S8 = 5, S9 = 5, S10 = 5 — 33 beats.** The maps are now binding; the presenter's essay pass remains the final voice authority later.

# 2. The Act II beat-state sheet — all 33 states, review only where design is live

Render every state through the states pipeline → `review/act-2/states/` (`sheet.html`, `states.json`), full-size, real assets, real type, composition law in full — with each cell carrying its provenance class and treated accordingly:

- **PORT states:** rendered from their named legacy treatments, verbatim (grade, type, and stage wiring only — geometry and design untouched). These are **approved by provenance** — `states.json` marks them `approved-port`, and the presenter's review of them is optional spot-checking, not a burden. Any port that cannot reproduce its legacy treatment without a design decision is a stop-and-flag.
- **ADAPT states:** rendered with exactly the one ruled change the map names, nothing else. Marked `pending-review` — the presenter verifies the change landed and nothing else moved.
- **NEW states:** rendered with **two or three genuinely distinct candidates each**, per the aesthetic law — including S7-F2 (the detachment: the certificate leaving the vault's orbit, dependency line back; candidates for the travel's geometry) and the map's other confirmed-NEW frames, using the systems sheet's existing candidates where the map says they apply rather than generating parallel new ones. Marked `pending-selection`.
- States that are pure script landings on already-approved compositions (statement lines over existing states) render as determined cells.

The claim through-line must be visible in the sheet: the cells show the held claim entering S5, wearing the carrier (construction C), persisting through every transformation to S10's strip — the connective object Gate 3 will animate.

# 3. Out of scope

No motion, no Gate 3, no implementation, no legacy-deck changes, no Act III work beyond §1.4's record, no new dark-field generation. Anything underdetermined: flag.

# 4. Verification (FAST scope)

Every cell through the pipeline's checks; the script installation's zero-difference diff; `npm run build` clean; deck boot smoke (39 slides, 0 errors); zero deck impact; commit granularity; clean tree; `act-2-states` tagged; report per AGENTS.md §16, mode named, with cells grouped by review class — **the presenter's review protocol: rule the `pending-selection` candidates and verify the `pending-review` adaptations; `approved-port` cells need him only if something looks wrong. His verdicts unlock Gate 3 (the claim threading the carriers) and then implementation.**
