# Act III — The Provenance Map
## Not yet a map. One standing pre-classification, recorded ahead of it.

**Status: A STUB.** Act III (Scenes 11–15) has no package, no scripts and no frame list, so it has no map. This file exists because `AGENTS.md` §4.9 says a frame's class lives with the act it governs, and the presenter issued one Act III classification early. Recording it where it will be looked for costs nothing now and saves it from being lost.
**Nothing here governs a frame that does not appear below.** When Act III's package lands, this document is completed the way `docs/act-2-provenance.md` was — proposed against the legacy deck, ruled in one pass, then binding.
**Date:** 31 August 2026

---

# 1. Standing pre-classification

| Frame | Class | Legacy source / ruling |
|---|---|---|
| the monetization-stages timeline | **ADAPT** | `src/slides/section-3-function/03-the-order-of-monetization.js` + `src/components/section-3/StageLadder.js`. **Ruling (presenter, 30 August 2026):** *basic icons replaced by dark-field renders at lineup scale, staged as an object sequence* |

**The one change, stated as it must be made:** the ladder's stage marks stop being grammar glyphs and become dark-field renders at **lineup scale**, and the stages read as **an object sequence** rather than as marks on a rising line. Nothing else in the treatment moves — not the ascent, not the thresholds, not the foundation state, not the attribution.

**The register boundary is respected, and the ruling says so explicitly:** renders never enter a diagram (master §6.3). Lineup scale is the register's own sanctioned scale for a row of objects — it is what `ComparisonAssetHeader`'s `DISPLAY_BOX` and the carrier lineup of `4-06` already use — and an object sequence is not a diagram. **This is the boundary the frame has to be built to clear**, and the session that builds it should treat clearing it as the frame's first test rather than its last.

---

# 2. One thing to resolve before this becomes a map

**Flagged, not resolved — the architecture retires the monetization ladder by name.** Its *"what dies"* list reads: *"the monetization ladder (→ coordination logic; Boyapati to notes)"*, and Scene 13 — *Different Jobs, Different Coordination* — is described as **replacing** it, with Boyapati's stages moving to the notes as historical corroboration.

So the pre-classification above and the architecture point at each other and need one word from the presenter:

- If the **stages timeline survives** as a frame somewhere in Act III, the ADAPT above is its class and the architecture's *"what dies"* line needs a corresponding amendment.
- If the **architecture stands**, the ADAPT is a class for a frame that will not be built, and it should be recorded as spent rather than carried forward.

**No session resolves this.** It is a freeze-register question, and it is raised here at the cheapest possible moment — before anyone builds against either reading. It is recorded in `docs/act-2-states-report.md` §7 as well, so it reaches him in a report rather than only in a file he has no reason to open yet.
