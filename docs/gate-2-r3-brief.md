# Gate 2 — Motion r3
## The return path's failure language, rebuilt as three live candidates · the spend goods contained

**Mode: FAST.** Two targets only; everything else changes zero pixels.
**Model: Fable 5, top effort. Fresh CLI session.**
**Branch:** `film-rebuild`. **Prior tag:** `gate-2-r2`. **Tag on completion:** `gate-2-r3`.
**First step:** commit this brief as `docs/gate-2-r3-brief.md` if not already present.
**Standing law applies in full**, including the full-coverage rule, with this iteration's controlled exception: the reopened states below are presenter-reopened, and their replacements are judged live as runtime candidates — his selection re-enters the approved record at the gate close.
**Read before working:** `docs/gate-2-r2-brief.md` and its report, the reopened cells at full size, `review/gate-2/r2/`'s strip, and the legacy spend reference (`review/gate-2/states/ref-4-05-*`, the spend builds).

---

# 1. The presenter's rulings (28 August 2026 — record first)

1. **The D3-C failure language is rejected in motion.** Viewed live, the gapped attempt (S2 b3), the stepped thinning (S2 b4), and the faded remnant at the birth (S3 b1) all read as rendering defects, not as a path failing. **Formally reopened:** `s2-b3-a`'s path element, `s2-b4-c`, and `s3-b1`'s remnant. The compositions around them (patient, surgeon, goods cluster, disc) are NOT reopened — only the path element within these states. Update `states.json` accordingly.
2. **`s4-b2-b`'s goods layout is reopened with an explicit markup** (§3): the three renders are too large and reach into the frame's center.
3. **Recorded from the live review by absence of objection:** the micro-motions are **kept** (R5 closed); the S2 entry is **accepted** and the presenter's original temperature concern is **resolved** (D8 closed).
4. The gate remains open; it closes only on the presenter's recorded word after r3.

# 2. Task 1 — three failure languages, runtime-selectable (`?proto=gate2&path=1|2|3`)

Build all three as genuinely distinct systems. Each must be **consistent across its three sites**: S2 b3 (the attempt while the goods hang as possibilities), S2 b4 (the failure that sets up the statement), and S3 b1 (what remains, and what the pool birth consumes). The principle behind all three, binding: **no degraded stroke ever appears in a settled frame** — no gaps mid-stroke, no stepped fades, no thinning. Failure is carried by motion, by absence, or by a medium that is not a line.

- **path=1 — The reach.** The return stroke is always clean and full-weight. In motion it extends from the patient toward the surgeon, decelerates, halts short, and withdraws part of the way. Settled frames show a whole short stroke with a terminal dot, and authored black between its end and the surgeon. At b4 the reach is longer and the retreat deeper — the second, harder try. At S3 b1 the settled remnant is the same clean short stroke; the pool consumes it.
- **path=2 — The absence.** No return line exists before the birth. The service path stays whole; the return direction is marked only by a terminal dot at the patient's edge that brightens, strains toward the surgeon, and subsides — twice, harder at b4. The goods hang dim in the unreachable middle. At S3 b1 the birth restages: the pool gathers from this absence (the dot's final strain feeding it) rather than from line fragments — the claim as the answer to a void. This candidate alters the birth frame's remnant area; capture it clearly.
- **path=3 — The flow.** The return attempt as a faint warm stream of light flowing from the patient toward the surgeon, stalling, and dispersing short of arrival. Austere — in the pool's family, never particle confetti. Settled frames show only a dim residual drift, no broken geometry. At S3 b1 the pool drinks the stream's residue.

Reduced-motion parity for each candidate: settled states reached instantly, identically. The statement beat (b5) and all other beats are untouched by all three.

# 3. Task 2 — the spend goods, contained (direct markup, no candidates)

In S4 b2 (system B's spend resolution): the shoe, steak, and wine render at a reduced, uniform scale, arranged as a **compact triangle** within the spend terminal's zone — wholly inside the frame's left third, never crossing toward center. Study the legacy deck's spend arrangement (`ref-4-05-*` spend builds) for the feel the presenter is pointing at; do not copy its geometry, match its containment and modesty. The claim's travel and redemption motion are unchanged. Re-render the affected settled state through the states pipeline as `s4-b2-b2.png` beside the reopened original.

# 4. Out of scope — hard

Every other beat, gesture, and settled state changes **zero pixels** — prove it: the landed-state comparison on all non-target beats must show zero differing pixels against r2's approved renders. No engine, deck, manifest, or script changes. No new compositions beyond the specified path element and the goods containment.

# 5. Verification (FAST scope)

- The zero-pixel proof on unchanged beats; the usual suite (traversal, cold entry, reduced-motion parity, console clean, build clean, deck boot smoke) across all three `path` values.
- Capture strips per candidate → `review/gate-2/r3/`: b3 settled + mid-gesture, b4 settled + mid-gesture, S3 b1 settled + pool mid-birth, for path=1, 2, 3; plus the contained `s4-b2-b2` settled and the claim's redemption mid-gesture.
- Commit granularity; clean tree; `gate-2-r3` tagged; report per AGENTS.md §16, mode named.

# 6. The review that follows (state it in the report)

The presenter runs `?proto=gate2&path=1`, `=2`, `=3` live, forward and backward, motion-on and reduced-motion, and returns: his path selection, his verdict on the contained spend goods, and — if it is now truly there — **the word that closes the gate: exceptional.** On the word: a closing FAST micro-session records the path selection, re-renders the affected cells as the new approved states, retires the alternates to file, and tags `gate-2-closed`.
