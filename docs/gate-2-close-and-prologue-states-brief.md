# Gate 2 Close · Prologue Beat-State Sheet
## Record the gate's closure, then bring P1 and P2 under the full-coverage rule

**Mode: FAST.** Records, re-renders, and stills — no motion work.
**Model: Fable 5, top effort. Fresh CLI session.**
**Branch:** `film-rebuild`. **Prior tag:** `gate-2-r3`. **Tags on completion:** `gate-2-closed` (after §1) and `prologue-states` (after §3).
**First step:** commit this brief as `docs/gate-2-close-and-prologue-states-brief.md` if not already present.
**Standing law applies in full**, including the full-coverage rule (docs/gate-2-states-brief.md header), which this session extends to the Prologue.
**Read before working:** `docs/gate-2-r3-brief.md` and its report (§6's closing protocol and the four flags), `docs/batch-a-package.md` §1–§3 (the P1/P2 beat maps, verbatim scripts, and frame specs), `docs/synthesis-architecture.md` (P1/P2), `docs/dark-field-manifest.md` (the assignments, the `bitcoin` V-1 flag at §223, the `fiat` gap at §3.0), and the approved stills `p1-f1`, `p1-f2`, `p1-f3`, `p2-f1-plain`.

---

# 1. Close Gate 2 (executes only with the presenter's word, supplied in the session prompt)

The presenter's rulings, given live against r3:

1. **The failure language is path 2 — the absence.** No return line exists before the birth; the terminal dot strains and subsides; the birth pools from the void.
2. **The contained spend goods (`s4-b2-b2`) are approved.**
3. **The gate's word:** as recorded in the session prompt. Without the word in the prompt, skip this section entirely, leave the gate open, and proceed to §3 only.

On the word:

- Record the path selection in `states.json` and a dated closing record in the states report; set path 2 as the prototype's default (all three remain runtime-selectable on file).
- Re-render the affected cells through the states pipeline as the new approved states: S2 b3, S2 b4, S3 b1 (path-2 settled states) and S4 b2 (`s4-b2-b2` promoted). Retire the alternates to file.
- Resolve r3 flag 1 as its report specifies: `s2-b5-b`'s context follows the selection — re-render its cell with the path-2 context so the approved record no longer speaks D3-C.
- r3 flag 2 (coherence work on the banked birth alternates) is explicitly deferred — pool is the ruled default; no work.
- Tag `gate-2-closed`. **Gate 2 is the film's signature system, closed on the presenter's recorded word — note it in the report as the milestone it is.**

# 2. Standing records to carry (no decisions here)

- R5 (micro-motions kept) and D8 (entry accepted, temperature concern resolved) were recorded closed at r3.
- The `fiat` render remains the presenter's generation task (manifest §3.0); nothing in this session waits on it.

# 3. The Prologue beat-state sheet — P1 and P2 under full coverage

Batch A implementation cannot animate P1/P2 until every beat state is a presenter-approved still. Produce the sheet exactly as the Gate 2 states session did: full-size 1920×1080 renders through the states pipeline, real assets, the deck's real type, all brightness and register law applied; output to `review/prologue/states/` with `sheet.html` (full-size links, no thumbnail judging) and `states.json`.

**The beat count comes from `docs/batch-a-package.md` §1's beat maps — the maps are binding.** The architecture's P1 summary compresses the morph into one beat while the verbatim script advances through each form; if the §1 map and the script's `[→]` count disagree anywhere, **flag the discrepancy in the report and render to the script's advance count** — one `[→]` = one state, per the script standard. Expected on that basis: P1 ≈ 11 states plus the entry black; P2 = 2 states. Number cells `p1-b1 …`, `p2-b1 …` from the first advance, with `p1-b0` for the authored entry black.

**Carried approvals:** `p1-f1`, `p1-f2`, `p1-f3` land as the states they were approved for (map them explicitly in `states.json`); `p2-f1-plain` is P2 b1. Re-render only if inputs changed; otherwise carry.

**Determined cells (render, no candidates):** the entry black; the hours-field fill states and the completed field with counter; the condensation's settled landing; the morph forms that have assigned renders — shell (`single_cowrie`), gold, paper note (`paper`), ledger (`ledger_glow` or `ledger` per the manifest's assignment — follow the recorded row, flag if ambiguous); the forms line; the question line; the title landing. Typography per the deck's system; where the legacy deck already renders an equivalent state (the hours field, the title), match its proven treatment.

**Candidate cells (presenter selects):**

- **C1 — The fifth form: bitcoin.** The library's `bitcoin` render is a physical coin, and the manifest flags exactly this (V-1): the film argues bitcoin has no physical form, and P1's line is "And lately — this." Two candidates: (A) the coin render, visually continuous with the four physical forms that precede it; (B) a non-physical treatment composed from the film's existing vocabulary — e.g. the form refusing to condense into an object: the material dissolving into structured light or ledger-like glow, distinct from the Claim Mark (which must not be anticipated here — the disc is born in Scene 3 and nowhere earlier). Candidate B must use existing register elements only; if it cannot be composed without new generation, render the closest honest version and flag it.
- **C2 — The counter treatment**, if the §1 map or frame specs leave it open (the counting typography against the filling field): two treatments, else render the determined one.

**Composition law:** every cell shippable as-is; no accent color anywhere in the Prologue (orange enters at Scene 3's birth); ~~no Claim Mark anywhere;~~ the self-reference ban ("presentation/slides/video" never visible); P1's register is monochrome plus the dark-field renders' photographic warmth per the visual journey.

> **STRUCK 30 August 2026 — presenter ruling (the restoration).** The clause *"no Claim Mark anywhere"* was **this document's own invention**, not the architecture's law. The record it claimed to restate — master §8.5 and the freeze register of 25 August 2026 — governs **the accent**, not the disc: *orange enters the film at Scene 3's birth; no accent color in the Prologue.* That is the law, and it stands unchanged. The struck clause is what made P1's condensation a design problem at all: it ruled out the one object the deck had already proven for that exact gesture. The full trail is `docs/gate-2-close-and-prologue-states-report.md` §12. **Do not reintroduce it from this document.**

# 4. Out of scope

No motion, no deck/engine/manifest changes, no Batch A implementation, no Act II work, no new dark-field generation. Anything underdetermined beyond the two candidate cells: flag, never improvise.

# 5. Verification (FAST scope)

- Grade/brightness checks on every rendered cell through the states pipeline; `npm run build` clean; deck boot smoke (45 slides, 0 errors); zero deck impact.
- For §1: the re-rendered cells pass the same pipeline; the prototype's per-path smoke on path 2 as default (traversal, cold entry, reduced-motion parity, 0 console errors).
- Commit granularity; clean tree; both tags; report per AGENTS.md §16, mode named, listing every cell's status and the review protocol: **the presenter reviews every cell at full size, per-cell verdicts, selections on C1 and C2; his approvals + Gate 2's closure unlock the Batch A implementation brief.**
