# P1 Restoration · The Provenance Rule · Act II Provenance Map
## The proven cold open returns verbatim · proven material becomes untouchable by law

**Mode: FAST.** A restoration, two records, and a mapping document — zero design decisions exist in this session.
**Model: Opus 5, max effort. Fresh CLI session.**
**Branch:** `film-rebuild`. **Prior tags:** `batch-a-r2`, `act-2-systems`. **Tag on completion:** `batch-a-r3`.
**First step:** commit this brief as `docs/p1-restoration-and-provenance-brief.md` if not already present.
**Read before working:** the legacy Prologue slides on disk (`1-01-eighty-thousand-hours`, `1-02-the-conversion`, `1-03-what-is-money` — the batch-a report's mapping table names them), the current P1 module, `docs/synthesis-architecture.md` (the "proven organs" clause), `docs/batch-b-package.md`, the act-2 systems sheet (context only — no work on it).

---

# 1. Records (first, each its own commit)

1. **The provenance rule (presenter-ordered, 30 August 2026), into AGENTS.md §4:** every frame and state in the remaining film carries a provenance class, recorded before any design or implementation session touches it: **PORT** — a proven legacy treatment exists; it is transplanted verbatim to the film (grade, type, and engine wiring only); redesign is forbidden and candidate generation for it is a process defect, unless the presenter explicitly reopens it by name. **ADAPT** — a proven treatment exists with one ruled change (the ruling named in the map); only that change is made. **NEW** — no proven treatment exists; the full-coverage rule and candidate process apply as established. Sessions verify the class before touching anything.
2. **The condensation ruling (presenter, 30 August 2026):** P1's condensation is **PORT** — the legacy deck's absorption is the proven treatment and is restored verbatim (§2). The Prologue composition-law clause "no Claim Mark anywhere" (introduced in the prologue-states brief) is **struck as over-extension** and replaced with the architecture's actual law: *orange enters the film at Scene 3's birth; no accent color in the Prologue.* Record both the strike and the replacement in the states report — the trail should show what was wrongly legislated and by which document, so it cannot silently return.

# 2. The restoration

Restore P1's condensation **exactly as the legacy deck performed it**: the field's points absorbed gradually into the luminous white ball, using the legacy slides' own gesture — port the legacy implementation (timing, easing, absorption behavior, the ball's rendering) rather than re-authoring it from description. The ball is the legacy ball, verbatim. The granular and white-monochrome versions retire to file with their cells marked superseded-by-restoration. Wire the restored gesture into P1's beat structure (the absorption spans its ruled advances; the first morph into the shell departs from the restored ball); re-render the affected cells through the pipeline **from the restored implementation** as the new approved states; reduced-motion parity; landed-state proofs on every changed P1 state; deck smoke (39 slides, 0 errors); capture the absorption at three points plus the landed ball into `review/batch-a/`. If any part of the legacy gesture cannot be ported without behavioral change (engine differences), reproduce its judged behavior exactly and state the mechanism in the report — the presenter's test is simple: it looks like it always did.

# 3. The Act II provenance map

Produce `docs/act-2-provenance.md`: every frame of Scenes 5–10 (per `docs/batch-b-package.md` §3, including the non-◆ frames) classified PORT / ADAPT / NEW, with, for every PORT and ADAPT row, the **named legacy source slide** and, for ADAPT, the **named ruling** that defines the one change (e.g. S6-F2: ADAPT — legacy periodic elimination, ruling: "compressed into one continuous beat in Scene 6"). Base classifications on the legacy deck's actual contents and the architecture's adaptation clauses; where a frame's class is genuinely arguable, mark it **ARGUABLE** with one line per side and no recommendation-as-decision. The existing act-2 systems sheet is referenced where relevant: its candidates remain on file and apply **only** to frames the presenter confirms as NEW. **This session decides no classifications finally — the map is a proposal for the presenter's one-pass review**, after which his ruled map governs all Act II work.

# 4. Out of scope

No design of any kind. No Act II scene or script work. No changes to the systems sheet. No engine changes beyond what the port strictly requires.

# 5. Verification (FAST scope)

§2's proofs as listed; commit granularity; clean tree; `batch-a-r3` tagged; report per AGENTS.md §16, mode named, containing: the restoration's mechanism note, the struck-and-replaced law trail, and the provenance map's summary table with its ARGUABLE items listed first — those are the only things the presenter needs to actually decide.
