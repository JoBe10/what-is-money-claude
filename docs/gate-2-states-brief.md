# Gate 2 — Beat-State Sheet Session
## Every state of Scenes 2–4 as a full-size still, with candidates at every open decision — before any motion iterates

**Mode: FAST.** Stills and records only; scoped verification.
**Model: Fable 5, top effort. Fresh CLI session.**
**Branch:** `film-rebuild`. **Prior tag:** `gate-2-r1`. **Tag on completion:** `gate-2-states`.
**First step:** commit this brief as `docs/gate-2-states-brief.md` if not already present.
**Standing law applies in full**, and this brief adds one rule that now governs the rest of the film:

> **THE FULL-COVERAGE RULE (presenter-ordered, 26 August 2026).** No scene animates until every one of its beat states exists as a still the presenter has approved at full size. Derivation of unapproved states in motion sessions is banned outright. Bulk approval is abolished: a cell the presenter has not examined at full size is *unreviewed*, never approved. Where a state involves an open design decision, the sheet carries 2–3 labeled candidates — a single silent choice is a defect. Where even candidates cannot be composed without a missing decision, stop and flag.

**Read before working:** `docs/gate-2-brief.md`, `docs/gate-2-r1-report.md` (the derived-composition table and flags — they are this session's target list), `docs/what-is-money-master.md` §3.5, §5–§7, `docs/batch-a-package.md`, the r1 capture strip in `review/gate-2/r1/`, and the approved stills in `review/frames-a/frames/`.

---

# 1. The presenter's rulings (26 August 2026 — record first, in docs and code)

1. **R1 — The interval splits into three advances.** SOMEONE ELSE / SOMEWHERE ELSE / LATER are each their own beat. **S3 is now 9 beats:** (1) birth, (2) because-someone-else, (3) patient exits, (4) SOMEONE ELSE, (5) SOMEWHERE ELSE, (6) LATER, (7) completion demonstration, (8) reset to held claim, (9) separation + unfinished lines. Amend the beat map in `docs/batch-a-package.md` §1 and insert the two additional `[→]` marks in the S3 script's interval paragraph — **zero word changes**, the paragraph divides at the three names. Record the amendment as presenter-ruled.
2. **R2 — The capabilities build moves into S2 beat 1**: the six-line list accumulates as one gesture under the sentences that name it. Beat 2 is the service delivered + the patient receiving. S2 stays 5 beats. Amend the beat map; the script is untouched.
3. **R3 — The birth treatment is pool** (`birth=3`). Record it as the default in the prototype; **condense is the named banked alternate** (presenter reserved the right to revisit); all three treatments stay runtime-selectable per the aesthetic law's file-keeping clause.
4. **R4 — The Scene 3 → 4 morph is confirmed.** Record; no authored-black variant is built.
5. **R5 — The claim's micro-motions (pulse, breath) are kept into r2**, to be judged in motion.
6. **Housekeeping, presenter-approved:** fix `docs/batch-a-package.md` §2's straight apostrophes to typographic (the install was already typographic and verified word-identical; the source now matches the script standard, master §3.5).

The r1 prototype's *code* is not re-implemented in this session (the 19-beat structure lands in motion r2, against this sheet). Only the records, the docs, and the birth default change.

# 2. The sheet — full state coverage

**Scope: 19 states** (S2 = 5, S3 = 9, S4 = 5), every one rendered at 1920×1080 with real assets, the deck's real type, and the brightness rules applied — under the same harness discipline as the approved frames. Output to `review/gate-2/states/`: one full-size PNG per cell, an index `sheet.html` linking every full-size image (the presenter reviews at 100%, never from thumbnails), and `states.json` recording per-cell status.

**Carried approvals** (already law, re-render only if their inputs changed):
- `s2-f1-final` → S2 beat 2's landed state (capabilities now land in beat 1 per R2 — beat 1's settled frame is s2-f1-final's composition; beat 2 shows the delivery/receipt state, a new cell).
- `s3-f1-final` → S3 beat 1. `s3-f2-a` → the LATER-landed state, S3 beat 6 (its two predecessors are new cells).
- **`s2-f2` is formally reopened** (presenter-ruled: the patient is photographic everywhere he appears; the goods composition is vetoed). It is superseded by this sheet's S2 beat 3–4 candidates and retired from the approved set — kept on file.
- `s4-f1` and `s4-f2-a` are **provisionally reopened** under D7 below: they stand as candidate A in their cells, against new candidates that must try to beat them.

# 3. The open decisions — candidates required (the D-list)

Every item below produces **labeled option cells** (`s2-b3-A.png`, `-B`, `-C`…). Candidates must be genuinely distinct compositions, not easing-level variations. The presenter selects; nothing here ships on session choice.

**D1 — The patient's recession (S2 beats 3–4).** He is the photograph, always. When the frame turns to the surgeon's wants, show 2 treatments of his recession: (A) dimmed in place to the legibility floor; (B) a deeper recede with scale/position shift toward the frame edge, per the layer-handoff exception. No glyph anywhere.

**D2 — The goods as possibilities (S2 beats 3–4).** Three candidates for shoe/steak/wine as dim possibilities: (A) clustered near the surgeon per the original spec — his wants, gathered at his side of the stage; (B) a horizontal band on the return path's line — the things the path is trying to reach; (C) one more arrangement of the session's design, distinct from both. The vetoed center-stack does not return.

**D3 — The failing return path (system decision, shown in context).** The r1 fragments read as a rendering glitch. Three candidates for the path-failure language, each shown in its S2 beat 4 context cell: fragments that fade and misalign as they break; a path drawn full then visibly losing continuity (gaps widening toward the patient); a path whose stroke thins and dies before arriving. The chosen language must also govern S3 beat 1's remnant (the birth consumes this same path) — render the matching S3 beat 1 variant for whichever candidates differ visibly there.

**D4 — Clean stages for statements (S2 beat 5, S3 beat 9).** The statement beats land over murk in r1. Two candidates each: (A) the prior layer hands off fully (recedes past the floor, recorded as a handoff) with the statement on effectively clean black plus the claim where present; (B) an authored partial recede that keeps one anchoring element legible. The statement is the largest thing in the frame in both (display rule).

**D5 — The completion demonstration (S3 beat 7).** The claim must never read as transforming into the shoe. Candidates: (A) the claim departs along its outgoing path and exits; the shoe arrives on a distinct return path; both visible in the settled frame with the claim gone; (B) a two-phase single-frame composition — claim mid-departure left, shoe mid-arrival right, motion implied by the trails; (C) session's own staging, distinct. In all candidates the settled state shows: shoe present, claim absent, the story legible from the still alone.

**D6 — The interval typography (S3 beats 4–6).** With the split ruled, each name lands as its own advance. Two typographic systems across the three cells: (A) the r1/s3-f2-a system (stacked, letterspaced, top-aligned right of the claim) with each line landing at full voice and demoting its predecessor; (B) a stronger composition of the session's design — larger scale, different spatial logic, same three landings. Render all three beats in both systems (6 cells).

**D7 — Scene 4, rebuilt to beat the old deck (all 5 beats).** **The bar: exceed the legacy deck's spend-or-save treatment**, which the presenter judges superior to r1. First locate and study the legacy spend/save slides in the 45-slide deck (main's manifest); capture their settled states as reference cells in the sheet (labeled REFERENCE, not candidates). Then two full compositional systems for S4 — each system rendered across all five beats (fork · spend resolution · reset · save hold · closing pair): (A) the r1/approved-frames system, upgraded — s4-f1 and s4-f2-a stand here, with the fork and reset beats brought to their standard; (B) a new system of the session's design that learns from the legacy treatment's strengths (its clarity of the two paths, its staging) without copying it. 10 candidate cells + references.

**D8 — The S2 entry temperature.** The presenter is concerned the opening tableau reads somber. In stills this session can address the patient's presence: render 2 crop/scale variants of the existing patient photograph (tighter crop reducing the gown's dominance; adjusted scale/position) as S2 beat 1 variants. Note in the report: further temperature work (entry motion — light rising, the path drawing itself — and any regenerated patient photo) belongs to motion r2 and the presenter's own generation respectively. Do not attempt image regeneration in-session.

# 4. Composition law for every cell

All cells obey the compositional standard in full: one idea per frame, the two-element budget, brightness floors, the display rule, settle budgets, negative space, orange only where the claim has been born (S2 cells carry **no accent**), registers per the boundary rule, self-reference ban. Every cell is a frame that could ship — no wireframes, no placeholder type.

# 5. Verification (FAST scope)

- All 19 base states + all option cells render clean at 1920×1080; `states.json` complete; `sheet.html` links every full-size image.
- `npm run build` clean; deck boot smoke (45 slides, 0 console errors); no deck/engine/scene files changed — allowed changes: `docs/` records, the birth default in the prototype, the capture/render harness under `review/`.
- Commit granularity; clean tree; `gate-2-states` tagged.
- Report per AGENTS.md §16, mode named, listing: every cell's status, every flag, and the review protocol below.

# 6. The review protocol (for the report to state to the presenter)

The presenter reviews every cell at full size and returns a per-cell verdict: **approved / approved-with-notes / redo / select A|B|C**. No bulk verdicts. Unexamined cells stay unreviewed and block motion on their beats. His verdicts + selections become the approved state set; motion r2 then implements the 19-beat structure connecting approved states only, on Fable, with derivation banned.

# 7. Out of scope

- No motion work, no proto re-implementation beyond the birth default, no engine or deck changes.
- No new dark-field generation (the patient photo stays as regraded; variants are crops).
- No script wording changes beyond the ruled `[→]` insertions and apostrophe fix.
