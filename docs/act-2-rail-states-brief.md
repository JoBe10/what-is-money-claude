# Act II Rail States
## The approved staging amendment, rendered: one rail, growing right — every settled state as a still

**Mode: FAST — and deliberately lean.** The presenter has ordered minimal scope: this session builds the rail states and does nothing else. Checks not listed in §4 are not run; adding unrequested work is a defect this session, same as skipping requested work.
**Model: Fable 5, top effort. Fresh CLI session.**
**Branch:** `film-rebuild`. **Prior tag:** `batch-b`. **Tag on completion:** `act-2-rail-states`.
**First step:** verify `docs/act-2-staging-amendment.md` is present (the presenter placed it — it is the approved authority for everything below; if absent, stop). Commit this brief as `docs/act-2-rail-states-brief.md`.
**Read before working:** the staging amendment (in full — it maps all 37 beats), the current Act II states record, the rails-law entry in AGENTS.md, the legacy rail builds (`2-04`, `2-06` — the proven grammar the spine extends), the icon grammar §4.5.

---

# 1. Records (first, granular commits, no prose beyond the trail)

1. **The staging amendment is presenter-approved verbatim (1 September 2026).** Freeze-register entry: Act II's visual anchor amended to the continuous rail per the map; narrative structure, scripts, and the 37 beats untouched.
2. **The survival-brightness ruling:** on the rail, an undefeated station holds full voice; fallen stations dim with their wounds. Shells read as alive until Zanzibar lands. Record beside the rails law.
3. **The regenerated `gold.png`** (untracked, 1672×941): gate and ingest through the standard harness; the re-measured framing row governs — **every box that displays gold derives from the new render's own aspect** (the ledger_glow precedent), across Act II and P1's morph form. The retired render to file. Re-render P1's gold-form cell from the new row and prove it landed (the one Batch A touch this session makes; nothing else in Act I moves).

# 2. The build — the rail world as stills

Build the rail composition system per the amendment's spine and render **every Act II settled state** (37) into `review/act-2/rail/` (`sheet.html` ordered by beat — the presenter will read it as a flipbook — and `states.json`):

- **Rail beats:** cumulative settled frames — every prior station visible, the new arrival at full voice, wounds and annotations beneath, the object band per the rails law with the new gold, the claim riding. The survival-brightness ruling applied throughout.
- **Featured moments:** Zanzibar at SHELLS, 1971 at LEDGER — the dated-fact typography staged at the station, on the rail.
- **Overlay beats:** carried from their approved cells unchanged (the periodic-table waves, the mass state, the vault, the chart, the network, palladium); render only their two seam states each — the rail receded beneath the overlay's entry, and the rail returned with the answer landed — so the sheet shows the complete visual sequence.
- **Statement beats:** the approved lines over the receded rail per the map.
- The rail must read as **one system at every state** — one grammar, one rhythm, one scale discipline from b1 of Scene 5 to b5 of Scene 10, where the same rail is read again as argument (gain/dependency pairs lighting, no second strip).

**This is staging the approved map, not open design.** No candidates. Where the map genuinely underdetermines a composition, flag it in the report in plain English and render the closest honest version — one, not three.

# 3. Out of scope — hard

No scene rewiring or implementation (next session, Opus). No motion. No deck, engine, or manifest changes. No Act I work beyond §1.3's one cell. No Act III. No deck-wide suites, no traversal, no reduced-motion passes — nothing in the deck changed, so there is nothing to re-verify there.

# 4. Verification (the complete list — nothing more)

Gate evidence for gold; the P1 gold-cell landed proof; the pipeline's per-cell checks on the new rail cells (the render harness's own gates — brightness, corners, composition floors); `npm run build` clean and one boot smoke (39 slides, 0 errors) to prove zero deck impact; commit granularity; clean tree; tag; report per AGENTS.md §16, mode named, **review protocol in plain English: the presenter walks `review/act-2/rail/sheet.html` top to bottom like a flipbook of the act — the single question at every frame is "can I follow money's history by eye alone?" — and returns either the go-ahead or the frames that break the thread.**
