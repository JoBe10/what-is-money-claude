# Act II States — r2
## Selections recorded · the periodic table restored beat for beat · the vault goes photographic · the rails carry real objects

**Mode: FAST.** Records plus ruled re-renders — one construction pass over the affected cells.
**Model: Fable 5, top effort. Fresh CLI session.**
**Branch:** `film-rebuild`. **Prior tag:** `act-2-states`. **Tag on completion:** `act-2-states-r2`.
**First step:** commit this brief as `docs/act-2-states-r2-brief.md` if not already present.
**Standing law applies in full** — provenance rule, full-coverage rule, aesthetic law, no-invention rule, session-sizing law.
**Read before working:** the states report and `states.json`, the presenter's ruled cells below, legacy `2-05-two-survivors.js` (the restoration's verbatim authority), the vault and gold_certificate register rows, `docs/icon-grammar.md`, the Act I photograph-plus-line frames (`s2-*` finals — the register precedent for §3).

---

# 1. The presenter's rulings (31 August 2026 — record first, each its own commit)

1. **Selections:** S6-F3 = `s6-b5-a` (the counted load). S7-F2 = `s7-b4-b`'s logic (the custody boundary crossed) **with the restage of §3**. S9-F1 = `s9-b1-a`. Non-selected candidates to file.
2. **Adaptations verified:** `s5-b6`, `s8-b2`, `s10-b3` (placement confirmed), `s10-b4`, and the evidence-grammar 1803 proof — all approved.
3. **Freeze amendment (presenter-struck):** architecture Ruling 3 — "periodic elimination compressed in Scene 6" — is **struck**. The periodic-table elimination returns to the legacy pacing: **one wave of eliminations per advance, exactly as `2-05` performs it, nothing inside any step changed one bit.** Record the strike in the freeze register with the trail (ruled 25 Aug, struck 31 Aug, presenter's grounds: the legacy treatment is proven and preferred).
4. **The rails law (presenter-ordered, film-wide):** simple icons retire from monetary-good stations everywhere. On every rail, strip, or timeline of monetary goods, the goods are carried by their **dark-field renders as an object band above the line at lineup scale**, while the drawn line, stations, and labels beneath keep the film's line grammar. **The CLAIM station is the exception: it carries the ClaimObject disc itself** — never a photograph, never an icon. Applies now to `s5-b5` and `s10-b1`/`s10-b2`, is already pre-recorded for Act III's monetization timeline, and governs all future rails. Record in AGENTS.md beside the register laws.
5. **The plain-English rule (presenter-ordered):** any text that asks the presenter for a decision — ARGUABLE rows, flags, open questions, review protocols in reports and sheets — is written in plain English: the question stated simply, options in full sentences. Compressed style stays confined to machine-facing sections. Record in AGENTS.md; violations are defects.
6. **`metals.png`** (presenter-generated, untracked in `assets/dark-field/`): gate and ingest per the standard harness; on pass it joins the shipping set assigned to S5's rail; on fail, file the evidence, flag the failing clauses, stub the rail position.

# 2. The Scene 6 restoration

Re-split the installed S6 script to the legacy advance structure using **`2-05`'s wording verbatim** for the elimination waves (the film's beats before and after — the SCARCITY IN MATTER landing, hard-to-create/hard-to-destroy, hard-to-move — keep their approved text). Update the frozen beat map to the resulting count; recompute and state the act's new total. Replace the compressed elimination cell(s) with one cell per restored wave, each rendered from the legacy build it corresponds to — these are PORT cells, approved by provenance, no review burden. The 1803 foreshadow line stays where the legacy script speaks it. `s6-b5-a` (the mass state) is unaffected and lands after the restored sequence per the map.

# 3. The vault restage (S7-b4)

The selected custody-boundary logic, rebuilt in the film's real register: the **photographic vault render** holding the gold, the **photographic gold_certificate render** detached and traveling outward across the boundary, a thin dependency line back to the vault — the Act I photograph-plus-line pattern, not a diagram; the drawn circle and ticket glyph retire to file. Composition law in full; the frame must sit at the standard of the Act I finals. Render as the new `s7-b4` cell, marked `pending-review` — the presenter verifies the restage landed at the standard he asked for.

# 4. The rails upgrade (s5-b5, s10-b1, s10-b2)

Apply the rails law: on S5's rail — cattle, salt, shells, iron carried by their renders in the object band (the same studies `s5-b4` uses), METALS by the newly ingested `metals` render (stub if §1.6 failed); on S10's strip — GOLD by its render, CLAIM by the ClaimObject disc, LEDGER by `ledger_glow`, BITCOIN by the `bitcoin` render per the C1 ruling; the drawn line, station marks, wound lines, and gain/dependency text beneath unchanged in grammar. `s10-b2`'s history line re-lands on the upgraded strip. Lineup-scale discipline and the register brightness rules apply; the band must read as one system across both rails. All three cells marked `pending-review`.

# 5. Out of scope

No Gate 3, no implementation, no other cells touched (zero-pixel proof on the untouched set), no Act III work, no engine changes.

# 6. Verification (FAST scope)

Changed cells through the full pipeline; the S6 script re-split mechanically diffed (legacy wording verbatim in the waves, approved wording verbatim elsewhere); zero-pixel proof on untouched cells; `npm run build` clean; deck boot smoke (39 slides, 0 errors); zero deck impact; commit granularity; clean tree; `act-2-states-r2` tagged; report per AGENTS.md §16, mode named, plain-English review section listing exactly the `pending-review` cells — **the presenter verifies the restored elimination (it should simply look like it always did), the vault restage, and the two rails; his verdicts close Act II's states and unlock Prototype Gate 3.**
