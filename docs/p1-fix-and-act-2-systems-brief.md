# P1 Condensation Fix · Act II Design-Systems Sheet
## The condensed hours get their object back (per the presenter's letter) · the act's five visual systems become candidates

**Mode: FAST.** One small construction fix + one candidate sheet — sized to the new session law.
**Model: Fable 5, top effort. Fresh CLI session.**
**Branch:** `film-rebuild`. **Prior tag:** `batch-a`. **Tags:** `batch-a-r2` after §2, `act-2-systems` on completion.
**First step:** commit this brief as `docs/p1-fix-and-act-2-systems-brief.md` if not already present.
**Standing law applies in full.** The presenter's condensation ruling arrives **as a letter (A or B) in the session prompt**; §2 executes per that letter. If the letter is absent, skip §2 entirely (leave P1 as shipped) and execute §3 only.
**Read before working:** `docs/batch-a-implementation-brief.md` and its report (the P1 build, the flags), the P1 approved cells, `docs/batch-b-package.md` §3 (the ◆ frames — this session's §3 scope), `docs/what-is-money-master.md` §5–§6, `docs/icon-grammar.md`.

---

# 1. Records (first, each its own commit)

1. **The session-sizing law (presenter-ordered, 30 August 2026), into AGENTS.md §4:** one major construction task per session. A batch is a sequence of small FAST sessions — each under an hour where possible, each tagged, each independently reviewable — plus one closing session for batch-level verification and the batch tag. Briefs that bundle multiple major constructions are process defects.
2. **The condensation ruling** (per the prompt's letter), recorded in `states.json` and the states report: the affected P1 cells (the condensation landing and any adjacent states showing the condensed mass before the first morph form) are presenter-reopened and superseded per §2.
3. The Batch A report's eight scene-test tensions and eleven flags: **presenter-reviewed, accepted as flagged** — no action in this session; script-related tensions ride to the essay-conversion step; the seam and hygiene items ride to their named batches.

# 2. The condensation object (executes per the letter)

**If A:** the condensation lands on the Claim Mark's disc — rendered through the `ClaimMark` component, warm, exactly the protagonist's object. Record in the freeze register as a presenter amendment: *the no-anticipation rule is amended for P1's condensation; the birth in Scene 3 is a knowing reappearance.* (Recorded plainly so the film's own law reflects the ruling.)

**If B:** the condensation lands on a smooth luminous ball that is deliberately **not** the mark: pure-white monochrome per P1's register — no warm tone, no orange anywhere near it, a tighter/cooler glow falloff than the ClaimMark's, sized per the reopened cell's box. It must read as premium and intentional as a still (scene-test Q11) and remain clearly distinct from the Claim Mark at a glance; state the distinguishing parameters (hue, falloff, core-to-halo ratio) in the report.

**Both cases:** re-render the affected cells through the states pipeline as the new approved states beside the retired granular originals; rebuild the condensation gesture so the collapse lands on the new object and the first morph (into the shell) departs from it; motion character preserved otherwise; reduced-motion parity; re-run the landed-state proof on every changed P1 state; capture the collapse mid-gesture and the landed object into `review/batch-a/`; tag `batch-a-r2`.

# 3. The Act II design-systems sheet (script-independent — no scene work)

Render the five ◆ candidate systems of `docs/batch-b-package.md` §3 as full-size candidate cells through the states pipeline → `review/act-2/systems/` with `sheet.html` and `systems.json`. These decide Act II's visual language ahead of the scripts; the presenter's selections pre-seed the full Act II beat-state sheet.

1. **S6-F2 — the elimination funnel** (the act's one new diagrammatic system; this frame sets the film's diagram grammar): two or three genuinely distinct systems — the element field collapsing stage by stage; a descending gate sequence; a third of the session's design. Line grammar only; content per the funnel's frozen sequence (gases escape, reactives corrode, radioactives lethal, the forge's two, scarcity's one).
2. **S10-F1 — the trade-off strip:** candidates for GOLD → CLAIM → LEDGER → BITCOIN with one gain and one dependency each; must rhyme with the film's existing line system; the `bitcoin` position follows the presenter's C1 coin ruling.
3. **S9-F1 — the network formation:** candidates for the central layer receding into a distributed validation network; line grammar; no advocacy vocabulary anywhere on the frame.
4. **S5-F3 — the evidence grammar:** candidates for the dated-fact treatment (Zanzibar as the specimen: place, date, the one-line fact) — this treatment returns for every dated fact in the film (1971, the chart's frame, palladium's 1803), so design it as a system, not a one-off.
5. **S8-F2 — the chart at film grade:** candidates for the four-currency purchasing-power chart restyled to the deck's system; **the data is frozen and untouchable**; candidates vary treatment only (axes, weight, labeling restraint, how the four lines land).

Composition law in full for every cell; candidates genuinely distinct; anything underdetermined flagged, never improvised.

# 4. Out of scope

No Act II scenes or scripts, no legacy-deck changes, no seam fixes, no Claim Mark component relocation (hygiene item rides), no engine changes.

# 5. Verification (FAST scope)

§2: changed-state landed proofs at zero pixels; deck boot smoke (39 slides, 0 errors); traversal across P1; reduced-motion parity on the rebuilt gesture; build clean. §3: every cell through the pipeline's checks; sheet complete. Commit granularity; clean tree; both tags; report per AGENTS.md §16, mode named, with the per-cell review protocol: **the presenter selects one system per ◆ frame at full size; his selections + the essay's Act II section unlock the Act II scripts and states sheet.**
