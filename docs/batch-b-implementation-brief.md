# Batch B Implementation — Act II Enters the Deck
## Two sessions under the sizing law: the scenes built, then the splice and the batch close

**Mode: FAST, both sessions.**
**Session 1 model: Fable 5, top effort** (it authors the act's new connective motion). **Session 2 model: Opus 5, max effort.** Fresh CLI session each.
**Branch:** `film-rebuild`. **Prior tag:** `act-2-states-r2`. **Tags:** `act-2-impl-1` after Session 1, `batch-b` after Session 2.
**First step (Session 1):** commit this brief as `docs/batch-b-implementation-brief.md` if not already present.
**Standing law applies in full** — provenance rule, full-coverage rule, rails law, sizing law, no-invention rule, plain-English rule.
**Read before working (both sessions):** the ruled Act II states (`review/act-2/states/`, post-r2), the installed scripts and frozen 37-beat maps in `docs/batch-b-package.md`, the legacy source slides named in `docs/act-2-provenance.md` (their builds are the motion authority for every PORT), the Batch A implementation report (the transplant discipline and splice pattern to follow).

---

# 1. The presenter's rulings (31 August 2026 — Session 1 records first, each its own commit)

1. **The strip's second station** — per the letter in the Session 1 prompt: **CERTIFICATE** = the station carries the `gold_certificate` render, relabeled **CLAIM ON GOLD** (all four stations photographic; the ClaimObject disc is never a station — it is the traveler); **DISC** = the staging stands as rendered. Re-render `s10-b1` and `s10-b2` accordingly; the disc-as-station variant retires to file if CERTIFICATE.
2. **The bitcoin glyph retires at display scale in Act II:** `s9-b2`'s entrant block carries the `bitcoin` coin render per the C1 ruling. Sweep Act II for other display-scale glyph stand-ins for the asset and apply the same change; drawn grammar (network nodes, station marks) is not the asset and stays drawn; anything ambiguous is flagged, not decided. Re-rendered cells join the act viewing's review.
3. **Gate 3 is collapsed into the implementation review (presenter process amendment):** the prototype gate for the architecture morph is struck from the Batch B pipeline; **the presenter's live viewing of the implemented act is the gate**, with the claim's connective journey (S5 entry → carrier → certificate travel → strip arrival) named as what he judges; disappointing seams are fixed by ruled iterations against the pinned states. Record the amendment in the package and the freeze trail.
4. The act's beat maps stand frozen at **37**; the states record post-§1 is the approved set for every landed-state proof below.

# 2. Session 1 — Scenes 5, 6, 7 built (tag `act-2-impl-1`)

Build `src/scenes/` modules for S5 (8 beats), S6 (9 beats), S7 (5 beats) against the approved cells:

- **PORT states carry their legacy motion verbatim** — transcribe each named legacy build's gesture (the competition rail's verdicts and register switch, the periodic table's wave-per-advance eliminations exactly as `2-05` performs them, the ladder-derived beats where the map names them). The presenter's test on every port: it moves like it always did.
- **New motion, authored at top effort — the act's signature seams:** the held claim entering S5 from S4's world and taking the drawn carrier (construction C, centered); the carrier's presence as the rail competition runs; S7's certificate travel out of the photographic vault with the dependency line drawing back (the restaged `s7-b4` is the settled state); the S5→S6 and S6→S7 transitions per the architecture's morph/cut rulings.
- Every settled state is its approved cell, transcribed from the states builders, proven at zero pixels. Scripts as notes, `[→]`-aligned. Scene contract in full (cold entry, reduced-motion parity, backward traversal, teardown). No manifest changes yet — the scenes run in place behind the existing count until Session 2 splices.
- Verification: landed-state proofs on all 22 built states; per-scene smoke; build clean; zero deck impact; capture strip started under `review/batch-b/`; commit granularity; tag; report with a plain-English handoff note for Session 2.

# 3. Session 2 — Scenes 8, 9, 10, the splice, the batch close (tag `batch-b`)

- Build S8 (5), S9 (5), S10 (5) the same way: severance and pattern motion ported from `2-07`/`2-08`; the S8 dissolve landing on MONEY BECAME INFORMATION; S9's hub-dissolving network (`s9-b1-a`) and the neutral entrant block with the coin; the strip's stations activating one at a time per the ruled staging; palladium's two beats against it; the exit question.
- **The manifest splice:** Scenes 5–10 enter in place of the legacy slides they supersede, mapping table per the provenance map's sources (section-2 slides `04`–`08`, the relevant section-3 slides per the map; ambiguous fates — waypoints, dividers, `2-04`'s featured-moment slot — flagged, never decided). Legacy files stay on disk. State old and new slide counts. Boundary navigation seamless both directions: Act I → S5, S10 → the surviving legacy deck.
- **Batch verification:** landed-state proofs across all 37 states; full deck traversal both ways; cold entry at every new build; reduced-motion parity; notes and self-reference scans; the eleven-question scene test on all six scenes, honest per-question verdicts, tensions flagged; the capture strip completed; commit granularity; clean tree; tag; report per AGENTS.md §16, mode named, plain-English review section.

# 4. Out of scope (both sessions)

No Act III work, no new design (the two §1 re-renders are ruled changes, not open questions), no engine feature changes, no legacy file deletion, no script wording changes.

# 5. The presenter's review (state it in Session 2's report)

**The act viewing is the gate.** Run the deck from Scene 5, forward at speaking pace, then backward; motion-on and reduced-motion. Judge, by name: the claim's journey across the act's seams; every port moving as it always did; the vault frame and both rails live; the coin everywhere the asset appears. The word that closes the batch is the presenter's; ruled iterations follow anything that disappoints.
