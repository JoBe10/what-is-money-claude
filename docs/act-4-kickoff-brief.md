# Act III Final Fixes · Act IV Foundation and States
## Three ruled fixes on the live act, then the frozen argument comes home — two sessions

**Mode: FAST, both sessions — lean scope.**
**Model: Opus 5, max effort, both. Fresh CLI session each.**
**Branch:** `film-rebuild`. **Prior tag:** `batch-c-r2`. **Tags:** `act-3-final` after Session 1's Part A + `act-4-foundation` at Session 1's close; `act-4-states` after Session 2.
**First step (Session 1):** commit this brief as `docs/act-4-kickoff-brief.md`.
**Read before working:** the live S12–S15 modules and cells, the installed Act III scripts, `docs/synthesis-architecture.md` (Act IV, Scenes 16–23, and the frozen-argument clauses), the legacy `section-4-ideal-store/` slides in full (they are Act IV's script and treatment authority), the master's protected lines and freeze register, the provenance and staging precedents (`docs/act-2-provenance.md`, `docs/act-3-kickoff.md`).

---

# SESSION 1 — PART A: the three fixes (presenter-ruled, 3 September 2026)

1. **S12 — the renders replace the words.** Each column: the header, then the render(s), nothing else — USD note under PRICED IN; ARS note under PAID IN; USD note + the real-estate render under SAVED IN. The USD/ARS/dollars/pesos text rows retire. "Argentina, five decades." stays; the principle statement (b4) stays. Re-render the cells; rewire the beats.
2. **S14 — the contradiction fixed (ruled wording change, recorded).** The landed line becomes: **"A monetary good is trusted to hold value before it is used to pay."** The spoken sentence in the installed script changes accordingly: *"A monetary good is trusted to hold value before it is ever used to pay — payments live on the far side of the gate."* replaces the "reaches everyday payments last" sentence; the rest of the beat's words unchanged. Amend script, cell, and scene.
3. **The pivot relocates to the act's exit (presenter structural ruling, recorded in the freeze register).** The triad-return beat with the store-of-value question moves from S14's final position to **after the tower's held question — the act's final beat.** Ruled bridge wordings (verbatim):
   - S14 now ends at: *"…points at one job: store of value. Then let's judge it there."*
   - The tower's opening line becomes: *"But before we judge it there, let me pull the rug slightly. Is the thing in your bank account actually the base good we'd be judging? Start from the top, where you live: the payment app."*
   - The relocated pivot, now the act's final beat, over the returned triad with STORE at full voice: *"So now we can ask it properly. What makes something a good store of value? That question — asked from first principles — is the rest of this story."*
   Beat maps amended (S14 = 3, S15 = 8 including the pivot coda); cells re-sequenced; the S15→Act-boundary exit now leaves on the question. Tag `act-3-final` when Part A's proofs are green.

# SESSION 1 — PART B: Act IV's foundation (tag `act-4-foundation`)

Act IV (Scenes 16–23) is the frozen, audited Section 4 argument re-homed — the film's most protected material. Produce its foundation exactly as the learned pipeline requires:

1. **`docs/act-4-provenance.md`** — every frame of Scenes 16–23 classified against the legacy `section-4` slides, with named sources. Expectation: overwhelmingly PORT (the 100-year test, the inversion, the failure typography, the ten properties, the comparison table with its frozen data and fifty-score single advance, the closing line); ADAPT only where the architecture names a change; ARGUABLE rows in plain English, one line per side. **The staging question answered first, in the map's §0:** Act IV's anchor per the architecture — the returned exchange frame from Scene 4 reconstructing at Scene 16 (the through-line's homecoming), the register shifting to the act's black/white/orange abstraction; the proven Section 4 treatments are their own world thereafter.
2. **The scripts installed** — the frozen Section 4 scripts into a `docs/batch-d-package.md`, **verbatim from the legacy slides**, with a mechanical adaptation ledger limited to: structural self-references replaced, re-homing seams (the architecture's named fusions for Scene 16 — the Scene 4 return, the generalization landing as recognition, the bisection crescendo verbatim), and the `[→]` maps derived from the legacy advance structure. **The protected lines are verified character-for-character** — the crescendo, "Don't trust. Verify.", the enlargement rung 4 phrasing — and the enlargement sequence's virginity checked (nothing before Scene 16 anticipates the generalization). Zero creative drafting: any seam the ledger cannot bridge mechanically is flagged in plain English for the presenter, never written.
3. Report with the map's ARGUABLE rows first, plain English; commit granularity; both Part A and Part B tags cut.

# SESSION 2 — the Act IV beat-state sheet (tag `act-4-states`)

Render every Act IV settled state per the installed maps into `review/act-4/states/` (flipbook + `states.json`), each cell by its provenance class: PORT cells from their named legacy treatments verbatim (`approved-port`, review optional), ADAPT cells with their one named change (`pending-review`), anything the map left ARGUABLE rendered per the presenter's answers to Session 1's report (if unanswered at launch: rendered at the map's stated default and flagged). The Scene 16 homecoming cells show the Scene 4 frame reconstructing with the claim held — the act's entry seam. No candidates unless the ruled map names a NEW frame; one honest render plus a plain-English flag for anything underdetermined. Verification lean as established: per-cell checks, build clean, boot smoke, zero deck impact, commit granularity, tag, report with **the flipbook protocol: the presenter walks the act as stills — the return, the test, the inversion, the failures, the requirements, the fifty scores landing at once, the closing line — and returns his verdicts on the pending set plus the go-ahead that unlocks implementation.**

# Out of scope (both sessions)

No Act IV implementation, no motion, no design, no Act I/II changes beyond Part A's named fixes, no touching the frozen data or protected lines beyond verbatim verification.
