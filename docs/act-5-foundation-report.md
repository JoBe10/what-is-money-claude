# Acts III–IV Final · Act V Foundation — Report (Session 1)
## Four rulings executed and proven on the live acts; the case and the close laid down without a word of drafting

**Mode: FAST — lean scope.** Part A ran exactly the proofs the brief names: the records' byte-identity against the prior tag on both sheets, the per-cell checks on both re-cut sheets, the landed-state proof on every changed state through the spliced deck, both acts' traversals both ways, the exit and both seams three ways each, reduced-motion parity on the changed scenes, the static gates, the production build. Part B is documents and one harness: zero deck impact. No deck-wide audits. Those are the GATE's.
**Branch:** `film-rebuild`. **Prior tag:** `batch-d`. **Tags cut:** `acts-3-4-final` (at the Part A proofs) · `act-5-foundation` (at this report).
**Date:** 3 September 2026.

---

# 1. The questions the map puts to you — plain English, first

The Act V provenance map (`docs/act-5-provenance.md`) is a proposal until you rule it. Six rows are ARGUABLE. For each, the question, the two readings, and the default Session 2 renders if you say nothing. One word per row closes it. The package is built on the defaults.

## Row 1 — Do the three entry lines become beats?

Three legacy slides speak a line before their first advance, over the state they are entered on. They are 4-17 (*"Fiat, holding two of the three jobs — and holding them extremely well."*), 4-18 (*"An asset's value, split into two parts."*) and 4-21 (*"One claim, and five places it could go."*). No Act IV source had such a line, so the question is new.

- **Reading A, the default.** Each line becomes its scene's first beat. One arrow is added at zero word changes. The viewer sees fiat standing alone, the equation's opening, and the lone claim as settled frames. Act V is 27 beats.
- **Reading B.** Each line folds into the first advance's words, and the legacy entry state is only passed through. Act V is 24 beats.

## Row 2 — Where is the falsifiability passage spoken?

It left the deck for the script at R7.4 and its legacy home is 4-20's closing passages, spoken over that slide's last frame with no advance.

- **Reading A, the default.** It is spoken over Scene 26's last frame, the coexistence law, with no advance. Its last sentence names *"Don't trust. Verify."* as a spoken mention, which the freeze allows.
- **Reading B.** It is spoken over Scene 23's fifty scores instead, because its last sentence points at the table. That reopens the Batch D package's Scene 23.

## Row 3 — Where does the rules line sit inside Scene 29?

Ruling 5 keeps *"Bitcoin does not fix its price. It fixes the rules through which the market discovers its price."* in Scene 29. The frozen 4-23 script does not contain it, so the package places it.

- **Reading A, the default.** Before *"The case is not that the price will behave."* The three sentences after it stay together.
- **Reading B.** After *"The monetary rules should be hard to move."* It then sits between that pair and *"That's the whole case, in eleven words."*

## Row 4 — Is Scene 28's field a port, or re-derived from P1's canvas?

- **Reading A, the default.** The field is legacy 4-22's `FixedSupplyField`, ported whole. The rhyme with P1 is already structural: both fields read one grammar in `UnitField.js`. Session 2 renders the cells from that implementation.
- **Reading B.** The field is re-derived from P1's canvas at its density and timing. That is a NEW frame on a treatment the argument freeze names, and the candidate process would apply.

## Row 5 — Do the savings vehicles stay renders in Scenes 24 and 25?

The brief's staging line puts the migration and the premium in the act's abstract register. The legacy carries gold, real estate, shares and the fiat note there as display-scale renders, under the rule that every display-scale monetary object is a render.

- **Reading A, the default.** The renders stand. The two scenes are already typography and line grammar with the objects where the legacy shows them.
- **Reading B.** The vehicles retire to compact marks in these two scenes, and renders enter the act only at Scene 26. Two ADAPT rows, one named change each.

## Row 6 — Is the silence its own beat at the close?

- **Reading A, the default.** The advance that leaves the case lands on black. The callback is spoken over it. The second advance lands *Thank you.* Scene 30 is 2 beats, the legacy's own count with the wayline build replaced by the black it started from.
- **Reading B.** The callback is spoken over the case's frozen frame and Scene 30 is one beat.

## Also yours, from Part A

- **The band's fit at Scene 23.** At the rails-law box the render band does not fit above the table with the closing line at its held position. This render keeps the band at the film-wide box and tightens the table's ten score rows from 60 to 48 pixels, with the heading row shrunk to its label. The alternative keeps the legacy pitch and shrinks the band to roughly 100 pixels. Both numbers are one attribute away. The kicker no longer stacks tight against the group labels; the band sits between them. The fiat note reads small in its box by the register's own framing.
- **The exit's two wiring numbers.** The base slab keeps its A position rather than rising toward center, and the held question's drop dissolves with the upper layers rather than staying. One number lifts the slab. One word keeps the drop.
- **Scene 16's entry.** The disc now appears at the base slab's center at its Act III size and rises to the fork's apex. The start point and size are wiring.

---

# 2. Part A — the four rulings, recorded and executed

All four were recorded first in master §13, each its own commit, before anything was touched.

1. **Scene 12 — the household frame retires.** The household sentence is spoken over beat 1 as the empty columns land. The text-only row is on file as `s12-b2-household`, its bytes carried. Scene 12 is three beats. One arrow removed at zero word changes. Act III is 25. The Argentina and principle cells carry their approved bytes under their new beat numbers. Scene 13's entry, which launched from Scene 12's old fourth beat, is re-pointed. The smoke caught that one as a console error before it shipped.
2. **Scene 23 — the table's headers become renders.** The five glyphs retire from the asset headings. The renders ride as a band above the table at the rails-law box, each over its own column, bottom-aligned on one baseline. The grammar beneath is untouched. The change is at the source: legacy 4-16 builds the band, the table component carries labels alone, the R7.4 header toggle is retired. `shares` was in the register already. S23-F2 is ADAPT. The two table cells re-rendered. The band's fit is flagged above.
3. **The act's exit lands on the tower.** The pivot coda keeps its words. On the final advance the tower recedes to its glowing base slab and *"What makes something a good store of value?"* lands beneath it. No return to the triad. The wording is recorded as protected, with the rationale. The triad-return cell is on file as `s15-b7-triad`. The new `s15-b7` is rendered from candidate A's own foundation slab and the deck's question register. S15-F3 is a new ADAPT row. Scene 16's homecoming morphs from the slab.
4. **The leftovers.** 3-00 and 3-04 are out of the manifest, their files on disk. The function section is gone. The deck is 32 slides, was 34. Act IV exits onto 4-17 and the seam is proven both ways. The Scene 16 join keeps the legacy *"And"*, closed.

# 3. Part B — the foundation, mechanical

- **`docs/act-5-provenance.md`** — every frame of Scenes 24–30 classified against the legacy sources with named slides and builds. **22 PORT · 1 ADAPT · 0 NEW · 1 retired**, six ARGUABLE questions at stated defaults, none opening a NEW frame. §0 answers the staging question first: the act's anchor is a descent, nothing grows and everything narrows, from the world's savings to one claim, one field, four lines, one line, black. The UnitField rhyme's source is recorded: both fields read one grammar, the fill fractions 0.8235 and 0.7419, P1's pitch 4.58 on 400 × 200, Scene 28's 102 × 62 on 7 × 5, the densities 80,000 and 35, both components at their blob ids.
- **`docs/batch-e-package.md`** — the Act V scripts installed verbatim from 4-17 through 4-23 and 5-01, extracted by the harness, not transcribed. **27 beats at the defaults.** The ledger is four entries: two self-references replaced, the rules line installed in Scene 29 as Ruling 5 names, the waypoint sentence cut from the close with nothing written in its place. The three entry beats are recorded as the legacy's own entry advance made explicit. Every seam the ledger could not bridge is flagged in plain English.
- **The protected lines, verified character for character** (`review/act-5/script-install.json`): the rules line identical, once in Scene 29; the *"preferred place to store the next unit of value"* line identical on screen at the frozen final frame; the callback and the covenant verbatim from the legacy; the marginal question on screen and spoken; the premium line's words identical with the on-screen dash unspaced; *"decided at the margin"* spoken only; *"Don't trust. Verify."* on stage in no Act V source. **The self-reference ban, checked hardest at the close:** Scene 30 names no medium, no sequel, no waypoint.

# 4. Files changed

| file | what |
|---|---|
| `docs/act-5-kickoff-brief.md` | committed as the first step |
| `docs/what-is-money-master.md` | four §13 rows, the rulings recorded first |
| `docs/batch-c-package.md` | §1 S12 = 3, Act III = 25, the coda's visual; §2 the S12 merge |
| `docs/act-3-provenance.md` · `docs/act-4-provenance.md` | S12-F1, S11-F1, the new S15-F3; S23-F2 PORT → ADAPT |
| `docs/synthesis-architecture.md` · `AGENTS.md` · `docs/dark-field-manifest.md` | Scene 12 and 15 entries, §7's continuation line, the shares row |
| `src/slides/section-4-ideal-store/16-the-comparison.js` · `src/components/section-4/ComparisonAssetHeader.js` · `AssetComparisonTable.js` · `src/styles/slides.css` | the band at the source, the label-only headings, the toggle retired, the pitch attribute |
| `review/act-3/` · `review/act-4/` | the harnesses amended, the cells renamed and re-rendered, both records re-cut |
| `src/scenes/act-3-the-jobs-of-money/_jobsStage.js` · `12-…` · `13-…` · `15-…` | the three-beat split, the exit state, the launch point |
| `src/scenes/act-4-the-store-of-value-test/16-return-to-the-open-exchange.js` | the entry from the slab |
| `src/slides/manifest.js` | the splice block; two imports out; the function section gone |
| `review/acts-3-4-final/` | the four Part A harnesses and their evidence |
| `docs/act-5-provenance.md` · `docs/batch-e-package.md` · `review/act-5/` | Part B |
| this report | the session record |

**Out of scope, changed anyway: none.** No frozen data, no wording beyond the four-entry ledger and the ruled merge, no Act I or II scene.

## Commits and tags

```
2372eba  docs                  the Act V kickoff brief
545e545  record(ruling 1)      Scene 12 — the household frame retires
32cdab3  record(ruling 2)      Scene 23 — the headers become a band of renders
e599ad7  record(ruling 3)      the act's exit lands on the tower
d1904b6  record(leftovers)     3-00 and 3-04 retired; the join keeps the And
52d5f93  package(batch-c)      S12 = 3, Act III = 25
3bbfd47  provenance(act-3)     S12-F1, S15-F3, S11-F1
3fb3882  provenance(act-4)     S23-F2 PORT → ADAPT
db836dd  architecture          Scene 12, Scene 15, AGENTS.md §7
77872f3  manifest(dark-field)  the shares row
38b14a8  legacy(4-16)          the band at the source
9d34a2b  cells(act-3)          the household on file, the exit rendered — 30/30
8324fed  cells(act-4)          s23-b5 and s23-b6 re-rendered — 27/27
3882a25  scenes(act-3)         Scene 12 at three beats, Scene 15's exit
4dc8d4e  scenes(act-4)         Scene 16's entry from the slab
8e3c5fb  splice                the survivors retired, 34 → 32
5422462  scenes(act-3)         Scene 13's launch point — the smoke's catch
5690ade  review(acts-3-4-final) the proofs                       ← tag acts-3-4-final
67e993c  provenance(act-5)     the proposed map
dde51d5  package(batch-e)      the scripts installed verbatim
5c2bbfe  review(act-5)         the installation harness, 33/33
```
Plus this report. **Tag `act-5-foundation`** at the report commit.

# 5. Validation

| check | result |
|---|---|
| **The records' byte-identity** (`proof-record-acts-3-4-final.cjs` against `batch-d`) | **84 files: 77 untouched · 4 carried · 3 re-staged · 0 failures** — the household row, the two renumbered Scene 12 cells and the triad coda carry their tag bytes; the exit and the two table cells differ; s12-b4 gone and accounted for |
| **Per-cell checks** | Act III **30/30** at 31 cells and 25 beats; Act IV **27/27** at 42 cells, the band probed |
| **Landed proof** (`proof-acts-3-4-final.cjs`) | **17/17 at zero differing pixels**, three comparisons each, mounted cold at the deck position in the 32-slide deck — S12 b1–b3, S15 b1–b7, S16 b1, S23 b1–b6; re-run after the Scene 13 fix |
| **Smoke** (`smoke-acts-3-4-final.cjs`) | 32 slides; Scene 12 at three beats, Scene 15 at seven; Act III 25 states and Act IV 42 states forward exact and backward monotonic; the exit on the slab asserted by state; the S15 → S16 seam and the S23 → 4-17 seam each three ways; **parity 24/24**; **0 console errors** |
| **Static gates** (`static-gates-acts-3-4-final.cjs`) | **29/29** — beats, both installers verbatim, the hinge question on stage and in the notes, *"Don't trust. Verify."* once across 84 reachable modules, the frozen blobs, the language sweep, the splice, the rulings at their sources, the retired cells at their shas |
| **Script installation, Act V** (`review/act-5/harness/script-install.cjs`) | **33/33** — words and characters against the sources with exactly the ledger applied; 27 arrows matching §1; the three entry beats; the ledger's four entries once each; Ruling 5's distributions; the protected lines with every difference reported; the rhyme by blob id; the close clean |
| `npm run build` | clean |
| **Granularity / tree / tags** | each ruling's record, each amendment, each cell session, each scene, the splice, the proofs, the map, the package, the harness and this report their own commits; the tree clean at every stop; both tags cut |

**One defect introduced and fixed in-session, recorded rather than smoothed.** Renumbering Scene 12 left Scene 13's entry morph launching from a state that no longer existed. The landed proof and the parity check use cold entries and passed clean. The smoke's forward traversal is what caught it, as a console error. The launch point is re-pointed and the smoke re-run green.

**Recorded, not widened.** The batch-c static gate asserts Scene 12 at four beats and the batch-d static gate asserts the survivors stay in the manifest. Both are their batches' evidence at their tags. Neither was re-run or edited. The R7.4 gate's check for the retired header toggle is superseded by ruling 2 and is the GATE's to re-baseline, as the Batch A report already recorded for that harness.

**Not run, and not claimed:** the deck-wide register, brightness and composition audits; the freeze-check suites; the second-window notes check. Those are the GATE's.

# 6. Flagged, not improvised

1. **The band's fit** (§1). One attribute on the slide root holds the tightened pitch; removing it restores the legacy rows.
2. **The exit's slab position and the drop** (§1).
3. **Scene 16's entry point and size** (§1).
4. **Everything in the map's §4**: the two self-reference substitutions for your word pass; *"in eleven words"* after fourteen; the passage's pointers at the table; the claim's last journey from one decision claim to three demand claims; the full clear before the case; the fiat render's box; the dead code in legacy 4-20; *"a silent Thank you"* against the spoken words; the frozen kicker at Scene 29.

# 7. Remaining judgment calls

- **The re-walk of Scenes 12, 15, 16 and 23** in the deck: the columns landing without the household row, the tower receding to its slab with the question beneath it, the disc rising out of the slab into the homecoming, the band above the table. The word that closes Part A is yours.
- **Your one pass on the map's six rows and the seams**, which unlocks Session 2.
- What FAST deferred to the GATE: §5's last row.

# 8. Recommended next step

**Your one pass on `docs/act-5-provenance.md` §2 and §4** — the six ARGUABLE rows and the seams, in plain words — which unlocks Session 2: the Act V beat-state sheet (`act-5-states`), the migration, the premium, the other assets, the marginal decision, the field returning, the case, the silence.
