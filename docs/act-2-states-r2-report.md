# Act II States r2 — Report
## The rulings recorded · the elimination restored beat for beat · the vault photographic · the rails carry real objects

**Mode: FAST.** Records plus ruled re-renders — one construction pass over the affected cells, exactly as the brief scoped it. Scoped verification: the changed cells through the full pipeline, the S6 re-split mechanically diffed at character strength, zero-pixel proof on every untouched cell, the production build, the deck-boot smoke. No deck-wide suites beyond the smoke's own full traversal.
**Branch:** `film-rebuild`. **Prior tag:** `act-2-states`. **Tag:** `act-2-states-r2`.
**Date:** 31 August 2026.

---

# 1. What you actually have to look at

This section is written under your plain-English rule, and it is short.

**Four cells carry a `pending-review` outline on `review/act-2/states/sheet.html`, and they are the only formal verdicts this session asks of you:**

1. **`s7-b4` — the vault restage.** The question: does this frame sit at the standard of the Act I finals? It is the custody-boundary logic you selected, rebuilt in the film's real register — the photographic vault holding the gold, the photographic gold certificate detached and traveled outward, one thin dependency line back to the vault, and the protected phrase beneath. The drawn circle and the ticket glyph retired to file.
2. **`s5-b5` — S5's rail under the rails law.** The question: does the object band of renders read as one system with the strip, and is the drawn record beneath — the line, the markers, the labels, the four wounds — exactly as it was? The band carries the same four contender studies s5-b4 uses, receded where their stations have fallen, and METALS is carried by the newly gated metals render.
3. **`s10-b1` — the strip under the rails law.** The same question as the rail's: one band, one scale, the drawn sentence beneath unchanged. GOLD and BITCOIN are carried by their renders (bitcoin per your C1 ruling), LEDGER by the glowing ledger study, and the CLAIM station carries the ClaimObject disc itself — never a photograph, never an icon.
4. **`s10-b2` — the history line, re-landed on the upgraded strip.** The only question: does the line still sit correctly under the band?

**One walk with no formal burden: the restored elimination, `s6-b2` through `s6-b7`.** These are ports, approved by provenance. Your test is the one you set at the strike: walked in order — the table rises, the gases, the corrosion, the radioactives, the shapeless, the furnace and the verdict, one wave line at a time — it should simply look like it always did.

**Your verdicts on the four cells close Act II's states and unlock Prototype Gate 3.**

---

# 2. The six rulings, recorded first, each its own commit

| # | Ruling | Where it is recorded | Commit |
|---|---|---|---|
| 1 | **The selections** — S6-F3 = `s6-b5-a` (the counted load) · S7-F2 = `s7-b4-b`'s logic with the §3 restage · S9-F1 = `s9-b1-a` (the hub dissolving); non-selected candidates to file | `review/act-2/states/states.json` `rulingsR2` | `fdfaf39` |
| 2 | **The adaptations verified** — `s5-b6`, `s8-b2`, `s10-b3` (placement confirmed), `s10-b4`, and the 1803 evidence-grammar proof | the same block, and the cells now carry `approved-adapt` | `141ad6e` |
| 3 | **The freeze amendment — Ruling 3 struck**, with the trail (ruled 25 Aug, struck 31 Aug, your grounds: the legacy treatment is proven and preferred) | master §13 (the freeze register), the architecture's ruling row, and `docs/act-2-provenance.md` — S6-F2 reverts to **PORT**; the map stands at 9 PORT · 2 ADAPT · 3 NEW | `736abb6` |
| 4 | **The rails law** — renders carry the goods as an object band above the line, film-wide; the CLAIM station carries the disc | `AGENTS.md` §6, beside the register conventions, with a cross-reference in master §6.3 | `3e41739` |
| 5 | **The plain-English rule** — decision-facing text in full sentences; violations are defects | `AGENTS.md` §4.10 | `c10d146` |
| 6 | **`metals.png` gated and ingested** — 5 clauses of 5 through the standard harness; the shipping set re-gated at **24 images, 120 checks, 0 failures**; measured framing row `[1.457, -0.4, -0.4]` in the register; assigned to S5's rail | `assets/dark-field/metals.png`, `src/dark-field.js`, manifest §2/§6, evidence under `review/act-2/` | `10dfca1` |

---

# 3. The Scene 6 restoration

**The script re-split** (`docs/batch-b-package.md` §1–§2, commit `9d8f89e`): S6 is now **9 advances** — the approved SCARCITY IN MATTER landing; the table-rise advance; the four elimination waves and the furnace arrow **verbatim from `2-05-two-survivors`' notes, character for character, typographic apostrophes included**; then the approved hard-to-create and hard-to-move beats unchanged. The 1803 foreshadow sits inside the furnace arrow, where the legacy speaks it. **The frozen beat map amends with it: Act II is 37 beats** (S5 8 · S6 9 · S7 5 · S8 5 · S9 5 · S10 5).

`review/act-2/harness/script-r2-diff.cjs` proves the split mechanically — **17/17**: the five wave advances character-identical to the legacy arrows, the three surrounding beats character-identical to the prior install, the table-rise advance a character-identical prefix of the prior install's compressed paragraph, and the other five scenes untouched at character strength.

**The cells** (commit `868061f`): the two compressed cells are superseded (preserved at tag `act-2-states`) and the elimination is six PORT cells, `s6-b2`…`s6-b7`, each rendered from the legacy build it corresponds to. The builder is `2-05`'s own `_build` + `_applyBuild` transcribed — the legacy DOM against the legacy classes, the kicker from build 1, `ElementGrid` at its own steps, **exactly one wave line visible per build** (the legacy's `setVisible(line, n === step)`), the verdict at build 6. Nothing inside any step changed one bit. The mass state you selected lands after the sequence as `s6-b9`, carried **byte-identical** from `s6-b5-a`; the claim-in-carrier GOLD beat renumbers to `s6-b8`, carried **byte-identical** from the r1 `s6-b4`.

# 4. The vault restage and the rails

**`s7-b4`** (commit `887e6b5`): the selected boundary logic in the film's real register — the vault render (4:5, the larger mass, the thing that stays) at the left, the gold-certificate render (4:5, smaller, at its distance, at full voice) at the upper right, one thin dependency line back in Act I's own service-path grammar (stroke 1.5 at 0.35, dot terminals r 3.5 at 0.7, the line clearing each photograph as Scene 2's service path clears its figures), and *A claim on gold in a vault.* as the statement. No kickers — the photographs carry their own identity, as the Act I finals do. Marked `pending-review`.

**`s5-b5` · `s10-b1` · `s10-b2`** (commit `54155ce`): the rails law staged. On S5's rail the component itself still draws everything beneath the line — the cell turns on the component's own render layer (`renders: true`, the same four contender studies), adds the METALS render box at the component's own band baseline (world −52) in the render's near-16:9 aspect, and applies the dimmed-prior voice to the fallen goods' renders (the recession the component only defines for contender mode, because renders never rode the drawn rail before this law). On S10's strip the drawn sentence is untouched beneath, and the band above carries `gold`, the ClaimObject disc at 116 (the CLAIM exception), `ledger_glow`, and `bitcoin` per C1 — each box the render's own aspect at the band's shared height, **244 stage px with a 68 px clearance, which is exactly the contender band's own scale at the rail's metals camera (188 and 52 world px × 1.3)** — so the band reads as one system across both rails by construction, not by eye. Receded stations sit at the deck's dimmed-prior step (0.58, above the 55% floor); the live station carries full voice. All three marked `pending-review`.

---

# 5. Files changed

## New
| file | what |
|---|---|
| `docs/act-2-states-r2-brief.md`, `docs/act-2-states-r2-report.md` | the brief as placed, and this report |
| `assets/dark-field/metals.png` | presenter-generated, gated 5/5, ingested |
| `review/act-2/grade-metals-drop.json`, `grade-shipping-set-r2.json` | the gate evidence (drop zone; the full 24-image set) |
| `review/act-2/harness/script-r2-diff.cjs`, `review/act-2/script-r2-diff.json` | the re-split proof, 17/17 |
| `review/act-2/harness/capture-states-r2.cjs` | the ruled re-render pipeline — renders only the ruled cells, carries selections by file copy |
| `review/act-2/harness/proof-r2.cjs`, `review/act-2/proof-r2.json` | the zero-pixel proof — 34 untouched · 3 carried · 10 re-staged · 0 failures |
| `review/act-2/states/s6-b5.png`…`s6-b9.png`, `s7-b4.png`, `s9-b1.png` | the restored builds, the restage, the carried selections |

## Modified
| file | what |
|---|---|
| `docs/what-is-money-master.md` | §13: the strike row with its trail; §6.3: the rails-law cross-reference |
| `docs/synthesis-architecture.md` | Ruling 3's row and Scene 6's beat line carry the strike (a presenter amendment — the freeze rules permit exactly this) |
| `docs/act-2-provenance.md` | S6-F2 → PORT; the struck ADAPT block kept as the record; the count now 9 PORT · 2 ADAPT · 3 NEW |
| `AGENTS.md` | §6 the rails law; §4.10 the plain-English rule |
| `docs/batch-b-package.md` | §1 the amended map (37); §2 the re-split S6 script; the S6 contract's compression clause marked superseded |
| `src/dark-field.js` | the measured `metals` framing row — the session's one `src/` change |
| `docs/dark-field-manifest.md` | the r2 drop block; the summary at 24 shipping, optional down to `gold-native` |
| `review/act-2/harness/states.mjs` | the restored builder, the selections applied, the restage, the rails staging, candidates re-classed `on-file` |
| `review/act-2/harness/check-states.cjs` | brought current with the rulings — see §7.4 |
| `review/act-2/states/s5-b5.png`, `s6-b2.png`–`s6-b4.png`, `s10-b1.png`, `s10-b2.png`, `sheet.html`, `states.json` | the ruled re-renders and the regenerated record |
| `review/batch-a/smoke-batch-a.json` | the smoke evidence, refreshed with metals live |

**Out of scope, changed anyway: none.** No Gate 3, no implementation, no engine change, no Act III work, no legacy-deck edit. Every untouched cell is proven untouched at byte strength.

## Commits and tag

```
b50f3b6  docs: the Act II states r2 brief arrives
fdfaf39  review(act-2): the three selections ruled - the counted load, the boundary logic, the hub dissolving
141ad6e  review(act-2): the five adaptations verified - the one ruled change landed on each
736abb6  docs: Ruling 3 struck - the periodic elimination returns to the legacy pacing, S6-F2 reverts to PORT
3e41739  docs: the rails law recorded - renders carry the goods above the line, the CLAIM station carries the disc
c10d146  docs: the plain-English rule recorded - decision-facing text in full sentences, violations are defects
10dfca1  assets: metals gated and ingested - 5 clauses of 5, the register is 24 and complete
9d8f89e  docs(batch-b): S6 re-split to the legacy advance structure - 9 beats, the waves verbatim, Act II is 37
868061f  review(act-2): the Scene 6 restoration - six legacy builds, one wave per advance, the counted load lands at beat 9
ddfd9ed  review(act-2): the verified adaptations take their class - approved-adapt, the record on each caption
980f919  review(act-2): the S9 selection applied - the hub dissolving carried byte-identical to s9-b1
887e6b5  review(act-2): the vault restage - the boundary logic in the photograph-plus-line register, pending the presenter's eye
54155ce  review(act-2): the rails law staged - the object band on S5's rail and S10's strip, the disc at CLAIM
f82cd47  review(act-2): the r2 verification - 12/12 sheet checks, zero-pixel proof 47/47, smoke green on 39 slides
```
Plus the report and handover commits. **Tag: `act-2-states-r2`** at the report commit.

---

# 6. Validation

| check | result |
|---|---|
| **The metals gate** | drop zone **5 clauses of 5** (corner 0.09, border 0.08, 82.1% dark, R−B +83.8, warm 99.5%, one key at spread 1); post-ingest full set **24 images, 120 checks, 0 failures** |
| **The re-split diff** | **17/17** — waves verbatim to the legacy at character strength, approved beats verbatim to the prior install, the other five scenes untouched, §1 = §2 = 37 |
| **Sheet checks** | **12/12** — map agreement (S6-F2 read out of the amended map), beat coverage 37/37 exactly once, one landed cell per NEW frame with candidates on file, frozen data, the register boundary as amended, the rails law at the source |
| **Cells measured** | **47/47** — corner 0.00, border 0.00, content present, 1920×1080 |
| **Zero-pixel proof** | **34 untouched byte-identical to the tag · 3 carried byte-identical (s6-b8 ← s6-b4, s6-b9 ← s6-b5-a, s9-b1 ← s9-b1-a) · 10 re-staged by ruling · 0 failures** |
| `npm run build` | clean |
| **Deck smoke** | 39 slides · 201-state traversal both ways · both splice boundaries · 33 cold entries · reduced-motion parity 33/33 · **0 console errors** — with `metals` live in the register |
| **Deck impact** | **two ruled changes and nothing else**: the `metals` framing row in `src/dark-field.js`, and the ingested render lighting the legacy 2-04's dormant featured-moment slot (§7.1) |
| **Commit granularity / tree** | each ruling and each construction its own commit; the tree clean at every stop |

---

# 7. Flagged, not improvised

1. **The metals ingest touches the legacy deck, by design.** The 2-04 competition record has carried a dormant featured-moment slot since R7.3 — `hasDarkField('metals')`, built to light itself the day the study landed. It now lights on the slide's final build. This is the recorded behavior of an approved mechanism, not a new composition; the smoke is green over it, and your eye passes it whenever you next watch the legacy stretch.
2. **The table-rise advance keeps the approved sentence pair, not the legacy intro.** The legacy's first arrow says "the salability competition," and your standing one-term ruling retired that label from Act II (master §3.4); its "zoom all the way out" transition also belongs to the dead rail world. So beats before the waves keep the installed approved text — which is the legacy intro's own words minus exactly those two things — and the waves themselves are legacy verbatim. The diff harness states this precisely: the strike's verbatim mandate is satisfied wave for wave, and the intro is the one advance where the approved install governs. If you want the legacy intro restored word for word, label included, that is one sentence to say and one small session to do.
3. **The restored cells render the table alone — no dimmed rail beneath.** The legacy slide raised the table over the rail scene; the rail as a persistent spine is dead by architecture, and the r1 sheet already established the grid-alone staging. Nothing inside any elimination step changed; what changed is only what the architecture already killed.
4. **`check-states.cjs` was amended, and this is a gate brought current, not widened.** Three edits, each encoding a ruling: the beat map at 37 (the strike), S6-F2 as PORT (the strike), and the register check's diagram list re-scoped (the rails law legalizes the band on `s5-b5`/`s10-b1`/`s10-b2`, and `s7-b4` is photographic by your restage order). A new check was added in the other direction: the strip must carry the band and the disc, and no station mark may survive in it.
5. **The CLAIM station's disc renders receded (0.58) on the strip**, per rule 10 — it is a prior station on both beats. The systems report's open question about whether CLAIM takes the accent **still rides**; the disc's own warm material reads muted at the receded step, and that is where your accent question will be answered, not here. The strip's other open register question — the bitcoin position — is now closed by C1 and the rails law: the coin render carries it.
6. **The mass-state candidates keep their r1 ids** (`s6-b5-a/b/c`) with their beat updated to 9 — the ids are the historical record of the sheet they were rendered on, and renaming files would have broken the byte-identity trail. The sheet's on-file section says this in place.
7. **`GOLD_ARRIVAL` in the legacy 2-05 stays off and untouched**, as it has been since R7.2. The restoration reads its notes as authority; it does not touch the slide.
8. **Everything the r1 report §7 sent to your eye or to the GATE rides on** — the Act III timeline flag, the vault-glyph note (now largely mooted by the photographic restage, but the icon set still has no vault mark), S8-F2's unexercised reservation, and the systems sheet's unused cells on file.

# 8. Deferred to the GATE

No deck-wide suite ran beyond the smoke's own full traversal: no landed-state proof (Batch A's 33/33 stands — nothing under `src/scenes/` changed), no brightness or composition audit over the legacy slides, no freeze checks, and `gates-r7-4.cjs` keeps its known stale baseline. **No gate was widened** — §7.4 records the one gate that was amended, and why.

# 9. Remaining judgment calls — all yours

- **The four pending-review verdicts** (§1) — the vault restage and the three rails cells.
- **The restored elimination walk** (§1) — no formal burden; it should simply look like it always did.
- **The Act III timeline flag** (r1 report §7.1) — still the one that blocks a future map rather than this one.
- **Whether the CLAIM station takes the accent** on the strip (§7.5) — rides to Gate 3, where the through-line moves.

# 10. Recommended next step

**Your four verdicts on `review/act-2/states/sheet.html`, plus the Scene 6 walk.** On your word they close Act II's states, and **Prototype Gate 3 — the architecture morph, the claim threading the carriers — unlocks.**
