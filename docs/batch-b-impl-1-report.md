# Batch B Implementation, Session 1 — Report
## The rulings recorded, and Scenes 5–7 built — the claim enters the act and the certificate leaves the vault

**Mode: FAST.** Scoped verification: the §1 re-renders through the states pipeline with a zero-pixel proof over every untouched cell, the 22 built states proven against their approved cells three ways, the route smoked end to end both ways with reduced-motion parity, the production build, and the deck boot proven unchanged. No deck-wide suites — the deck was not touched, and the batch's own full verification is Session 2's.
**Branch:** `film-rebuild`. **Prior tag:** `act-2-states-r2`. **Tag:** `act-2-impl-1`.
**Date:** 31 August 2026.

---

# 1. The plain-English handoff — what stands, and what Session 2 does

**This session asks nothing of the presenter.** The four §1 rulings are recorded and executed, and by his own ruling 3 the place his eye meets this work is **the act viewing after Session 2** — the deck run from Scene 5, forward at speaking pace, judging the claim's connective journey by name.

**What stands after this session:**

- **The strip's second station is the CERTIFICATE.** The letter arrived in-session (the Session 1 prompt's placeholder came through unfilled as "[CERTIFICATE or DISC]"; the presenter supplied CERTIFICATE when asked — the provenance is recorded in `states.json` `rulingsImpl1`). The station carries the `gold_certificate` render relabeled **CLAIM ON GOLD**, all four stations are photographic, and the disc is never a station — it is the traveler. The disc-as-station staging is retired to file, byte-identical.
- **The bitcoin glyph is retired at display scale in Act II.** The entrant block carries the coin render per C1; the sweep found no other display-scale glyph stand-in for the asset among the act's beat cells, and nothing ambiguous.
- **Gate 3 is collapsed into the implementation review.** The act viewing is the gate; its questions moved to the viewing intact (package §4, master §13).
- **The record is the approved set at 37 beats.** No cell is pending; the four r2 verdicts landed with the brief.
- **Scenes 5, 6 and 7 are built and proven** — every settled state its approved cell at zero differing pixels — and run as one continuous world at **`?proto=act2`**, behind the untouched 39-slide deck.

**Session 2 (Opus 5, max, FAST, fresh CLI):** build Scenes 8–10 the same way against the same stage; splice all six into the manifest in place of the legacy slides the provenance map names (legacy files stay on disk); run the batch verification across all 37 states, the full deck both ways, the eleven-question scene test; complete the capture strip; tag `batch-b`; report with the review protocol — and then the presenter watches the act, which is the gate.

---

# 2. The four rulings, recorded first, each its own commit

| # | Ruling | Where it is recorded | Commit |
|---|---|---|---|
| 1 | **The strip's second station — CERTIFICATE.** CLAIM ON GOLD carries the `gold_certificate` render; all four stations photographic; the disc never a station — the traveler. `s10-b1`/`s10-b2` re-rendered; the disc staging retired to file as `s10-b1-disc`/`s10-b2-disc` (byte-identical); the rails law's CLAIM-station exception superseded with the trail | `AGENTS.md` §6 · master §6.3 · manifest §2/§2.2 · package §5 · `states.json` `rulingsImpl1` · the amended rails-law gate | `d472f30` |
| 2 | **The bitcoin glyph retires at display scale in Act II.** The entrant block carries the coin render per C1 (`s9-b2`–`s9-b4` re-rendered; the derived head-box geometry recorded at the call site); sweep clean — drawn grammar stays drawn, on-file candidates keep their glyphs | manifest §2.2 (the V-1 block's extension) · package §5 · `states.json` · the cells' sources | `6f2f2e2` |
| 3 | **Gate 3 collapsed into the implementation review** — the act viewing is the gate, the claim's connective journey named as what he judges; seams fixed by ruled iterations against the pinned states | package §4 (the struck gate, its questions kept) · master §13 (the freeze trail) with a note on §14's list | `95e41dc` |
| 4 | **The beat maps stand frozen at 37; the post-§1 record is the approved set.** The four r2 pending verdicts land: `s7-b4` approved as the restaged selection, `s5-b5` as the rails-law port, `s10-b1`/`s10-b2` re-staged by ruling 1 | `states.json` `approvedSet` (37 beat cells + the 1803 proof) · the sheet's narrative · the review classes | `3440355` |

**Proven at the rulings:** 12/12 sheet checks (two gates brought current with the rulings, named in §6 — neither widened) · 49/49 cells measured · zero-pixel proof **42 untouched · 5 re-staged · 2 carried · 0 failures** against `act-2-states-r2`.

# 3. The build — Scenes 5–7 as one continuous world

`src/scenes/act-2-the-architecture-of-money/` — `_architectureStage.js` (the shared stage), `_sceneModule.js` (the contract), and the three scene modules, registered at `?proto=act2` while they wait for the splice. **No manifest change; the deck is untouched at 39.**

**Every settled state is its approved cell, transcribed.** The geometry, the style strings (concatenation order included — a duplicated cssText property resolves to the later declaration, so the order is part of the geometry), and the component mounts come from `review/act-2/harness/states.mjs`; the stage root carries the legacy section classes so the legacy stylesheet does the placing, and the shared rasterHint holds the rasterization contract. The landed-state proof closes it mechanically: **22/22 at zero differing pixels, three comparisons each.**

**Ports move like they always did.** The verdict landings and the row-to-record transformation are `EvolutionRail`'s own state transitions and camera move, at the states the legacy `2-04` drives; the elimination is `ElementGrid` behind `2-05`'s own DOM — one wave per advance, the waves' own auto-timing, one wave line at a time, the verdict with the furnace (Ruling 3's strike honored: nothing recompressed); the through-line composition is `ClaimObject` in `CarrierShell` exactly as `4-06` stages them, their own reveals carrying every return.

**The new connective motion — where this session's effort went:**

- **The claim's entry (S5).** The held claim finds its light at Scene 4's resting place (1530, 630 — `_exchangeStage.js`'s own `markSaveB`), crosses into the act, and construction C's carrier forms around it. The traveler is the same component at the same 116, so the closing handoff to the in-carrier claim is a same-pixel swap inside one synchronous callback.
- **The carrier's presence as the competition runs (S5).** The composition withdraws as the row rises where it stood; after the receipt, the same claim returns — first the thing inside, then the body's wall — under the closing pair. The carrier is present at the record's ends, exactly as the approved cells stage it; between them the record holds the frame alone, as the cells require.
- **The transformation under the rails law (S5 b4→b5).** The component's own camera move and defeats, with the renders staying on the band — receding to the dimmed-prior step with their stations instead of collapsing into marks — and METALS rising with its render.
- **The evidence landing (S5 b5→b6).** The dated fact lands in the severance's own reveal character: place and date rise together, the consequence follows at the legacy's own delay.
- **The certificate travel (S7 b3→b4).** The study becomes the photograph mid-frame (a same-pixel swap of identical boxes); the vault settles into custody — the heavy thing that stays — while the certificate leaves its orbit and travels outward across the boundary; the dependency line **draws back from the claim to the vault**, dot-terminated, in the service path's exact grammar; the protected phrase lands.
- **The boundaries.** S5→S6 is an authored cut-with-crossfade (the question changes — the composition clears as the gold study reveals, never through black); S6→S7 is a morph (the idea persists — the mass diagram recedes and the composition it interrupted returns under gold's recorded wound). Both judged from the paradigm rule; see §6.3.

Scripts installed as notes, verbatim from package §2 by mechanical injection (8 + 9 + 5 arrows), `[→]`-aligned with the first arrow spoken over the scene's entry. Scene contract in full: cold entry gestures, morphIn per boundary, backward handoffs, interrupted-gesture completion, reduced-motion parity, teardown.

# 4. Files changed

## New
| file | what |
|---|---|
| `docs/batch-b-implementation-brief.md`, `docs/batch-b-impl-1-report.md` | the brief as given, and this report |
| `src/scenes/act-2-the-architecture-of-money/_architectureStage.js`, `_sceneModule.js` | the act's shared stage and contract |
| `src/scenes/act-2-the-architecture-of-money/05-…js`, `06-…js`, `07-…js` | Scenes 5–7 |
| `review/act-2/harness/capture-states-impl1.cjs`, `proof-impl1.cjs` | the §1 re-render pipeline and the zero-pixel proof |
| `review/act-2/states/s10-b1-disc.png`, `s10-b2-disc.png` | the retired disc staging, carried byte-identical |
| `review/act-2/proof-impl1.json` | 42 untouched · 5 re-staged · 2 carried · 0 failures |
| `review/batch-b/harness/…` + evidence | the landed-state proof (22/22), the smoke, the capture strip (22 states + 13 gestures) |

## Modified
| file | what |
|---|---|
| `AGENTS.md` §6 · master §6.3 | the rails law's station clause amended by the CERTIFICATE ruling, trail kept |
| master §13 + §14 | the Gate 3 amendment row; the gate list annotated |
| `docs/batch-b-package.md` | §4 the struck gate; §5's certificate and bitcoin rows; the pipeline line |
| `docs/dark-field-manifest.md` | §2/§2.2 assignments; the V-1 block's Act II extension |
| `review/act-2/harness/states.mjs` | the certificate strip (disc staging kept as `stripDisc`), the coin entrant, the review classes |
| `review/act-2/harness/check-states.cjs` | two gates brought current (§6.2) |
| `review/act-2/states/` (5 re-rendered + sheet + record) | the §1 record |
| `src/proto/registry.js` | the three scenes at `?proto=act2` (leaves the list at the splice) |

**Out of scope, changed anyway: none.** No manifest change, no engine change, no legacy edit, no S8–S10 work beyond the ruled cell re-renders.

## Commits and tag
```
d2779dd  docs: the Batch B implementation brief is on file
d472f30  review(act-2): ruling 1 - the strip's second station is the CERTIFICATE
6f2f2e2  review(act-2): ruling 2 - the bitcoin glyph retires at display scale
95e41dc  docs: ruling 3 - Gate 3 collapsed into the implementation review
3440355  review(act-2): ruling 4 - the post-1 record is the approved set at 37 beats
4037ef3  scenes(act-2): the shared stage, and Scene 5 - the claim enters the act
f476faf  scenes(act-2): Scene 6 - the elimination runs at the legacy pacing
fb85eca  scenes(act-2): Scene 7 - the certificate travels out of the vault
be79b6e  review(batch-b): the landed-state proof - 22/22 approved cells at zero pixels
da97322  review(batch-b): the smoke - zero deck impact, 22 states both ways, parity 22/22
e288673  review(batch-b): the capture strip started - 22 settled states, 13 gestures mid-flight
```
Plus this report and the handover. **Tag: `act-2-impl-1`** at the report commit.

# 5. Validation

| check | result |
|---|---|
| **Zero-pixel proof (the record)** | 49 files: **42 untouched byte-identical to `act-2-states-r2` · 5 re-staged by §1 ruling · 2 carried byte-identical · 0 failures** |
| **Sheet checks** | **12/12** — map agreement, beat coverage 37/37, NEW discipline, frozen data, the register boundary, the amended rails-law gate |
| **Cells measured** | **49/49** — corner 0.00, border ≤ 0.07, content present, 1920×1080 |
| **Landed-state proof (the scenes)** | **22/22 at zero differing pixels** — scene↔archived cell, fresh cell↔archived, scene↔fresh cell, every state |
| **Smoke** | deck boots unchanged (39 slides, first id, 0 errors) · route boots 3 scenes · **22-state traversal exact forward, monotonic backward** · all four boundary handoffs land the previous scene's end state · **22 cold entries settled** · **reduced-motion parity 22/22** by serialization · **0 console errors** |
| `npm run build` | clean |
| **Commit granularity / tree** | each ruling and each scene its own commit; the tree clean at every stop |

**Not run, and not claimed:** no deck-wide register/brightness/composition audit, no static-gate suite (nothing in the deck moved), no second-window notes check, no scene test (Session 2's, on all six scenes). FAST defers the global suites to Session 2's batch verification and the GATE.

# 6. Flagged, not improvised

1. **The ruling letter's provenance.** The prompt's placeholder arrived unfilled; the presenter supplied **CERTIFICATE** in-session, and the record says so (`states.json` `rulingsImpl1.stripStation.provenance`) — the trail is explicit rather than smoothed over.
2. **Two gates amended, not widened.** `check-states.cjs`: the rails-law source check now asserts the certificate station (its prior form asserted the disc the ruling superseded), and the review-class protocol carries `ruled-re-render`. Both encode rulings; nothing was loosened to pass.
3. **The in-act boundaries are judgments from the paradigm rule, for his eye at the viewing.** The architecture rules the paradigm (*morph when the idea persists; cut when the question changes*) but does not name S5→S6 or S6→S7 individually. This session judged **S5→S6 a cut** (the exit question is answered by a new subject — the winner) and **S6→S7 a morph** (gold's weight persists across the boundary). If either seam disappoints at the viewing, it is one ruled iteration against the pinned states.
4. **The entrant head's box is derived geometry, recorded.** Ruling 2 fixes the treatment (the coin, per C1), not the box. The render stands at the act's one recorded lineup scale (the rails-law band height, 244) in its own 4:3, bottom-anchored where the retired mark's box ended, so the drawn rhythm beneath is untouched. Recorded at the call site in `states.mjs`; nothing else in the session was derived.
5. **S6's notes are the script alone.** Package §2's S6 block opens with a bracketed re-split annotation (the package's record, not spoken material); the scene's notes carry the nine `[→]` paragraphs only. Session 2's verbatim-script gate should compare the arrow blocks, and this is the recorded reason.
6. **The vault study's box is the approved cell's.** `s7-b3` renders the 4:5 vault in the builders' default landscape study box; the cell is approved as rendered, so the scene transcribes it. The travel gesture then swaps to the 4:5 photo box mid-morph by design.
7. **The legacy `2-08` entrant still shows its drawn glyph in the deck** until Session 2's splice removes the slide — the sweep's scope was the act's record and scenes, and the deck was not this session's to touch.
8. **The claim's strip arrival is Session 2 motion.** Under the CERTIFICATE ruling no settled S10 still carries the disc; the traveler's arrival at the strip — the last leg of the journey ruling 3 names — is built with Scene 10.
9. **The S4→S5 seam lands at the splice.** S5's entry starts the claim at Scene 4's own resting coordinates, so the cut boundary reads as the same object carried over; the seam itself is walked when Session 2 splices S5 after S4 (different scene groups — the engine's standard crossfade, with the entry gesture as its language).
10. **Everything the r2 report sent onward still rides** — the Act III timeline flag, S8-F2's unexercised reservation, the CLAIM-accent question (now mooted in its station form by the CERTIFICATE ruling; whether the *traveler* takes emphasis on the strip is Session 2's staging against the pinned states), and the systems sheet's unused cells on file.

# 7. Remaining judgment calls — all his, all at the viewing

- **The act viewing is the gate** (ruling 3): the claim's journey across the seams — the S5 entry, the carrier handoffs, the certificate travel, the strip arrival — plus every port moving as it always did, the vault frame, both rails, the coin everywhere the asset appears.
- The two boundary judgments (§6.3) and the entrant head's derived box (§6.4), should either read wrong in context.

# 8. Recommended next step

**Session 2** (Opus 5, max, FAST, fresh CLI): Scenes 8–10 against the same stage, the manifest splice, the batch verification, the completed strip, tag `batch-b` — then the presenter's viewing of the act, which closes the batch on his word.
