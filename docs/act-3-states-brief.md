# Act III — Rulings, Scripts, and the Beat-State Sheet
## The LADDER amendment recorded · the scripts installed · the triad-anchored act rendered as stills

**Mode: FAST — lean scope:** checks not listed are not run.
**Model: Fable 5, top effort. Fresh CLI session.** (The triad world and the upgraded ladder are the act's staging craft — the one Fable spend Act III needs.)
**Branch:** `film-rebuild`. **Prior tag:** `batch-b-r2`. **Tag on completion:** `act-3-states`.
**First step:** verify `docs/act-3-kickoff.md` and `docs/batch-c-scripts-draft.md` are present (the presenter placed both); if either is absent, stop. Commit this brief as `docs/act-3-states-brief.md`.
**Standing law applies in full** — provenance rule, full-coverage rule, rails law (it governs the ladder's stage marks), the every-station-arrives-with-a-line rule where rails appear, plain-English rule, session-sizing law.
**Read before working:** the kickoff (staging and provenance), the scripts draft, `docs/act-3-provenance.md`, the legacy sources (`3-01`, `3-02`, `3-03` + `StageLadder`, `3-06`), the Act II rail builders (the register precedent for object bands and featured moments), the coffee_cup and assigned renders.

---

# 1. The presenter's rulings (2 September 2026 — record first, granular commits)

1. **LADDER (freeze amendment).** The architecture's "what dies" entry retiring the monetization ladder is **amended**: Scene 13 is the ladder — legacy `3-03`'s treatment and script restored, **Boyapati attributed on screen**, the standing icons-to-renders ADAPT applied (stage marks become dark-field renders at lineup scale, an object sequence, nothing else in the treatment moves — the boundary test in `docs/act-3-provenance.md` §1 is the frame's first test). The coordination logic survives *inside* the scene as its proof; Boyapati-to-notes is struck. Record the full trail.
2. **The social-technology beat (presenter-ruled):** Scene 13 beat 4 as drafted — money as the one good used because everyone else uses it — in plain words; the vocabulary (network effects, Schelling point, salability) confined to the notes armor per the terminology law, which stands unamended.
3. **The staging map and provenance sketch in `docs/act-3-kickoff.md` are presenter-approved:** the triad is the act's home base per its §2; complete `docs/act-3-provenance.md` from the §3 sketch as the ruled map (all PORT/ADAPT; the coordination-scales NEW row recorded as not built).
4. **The scripts are approved as drafted.** Install `docs/batch-c-scripts-draft.md` into the Batch C package (create `docs/batch-c-package.md` §1–§2 from the kickoff's structure), mechanically diffed at zero word differences, notes-armor blocks installed as notes-only; **beat maps frozen: S11 = 5, S12 = 4, S13 = 6, S14 = 4, S15 = 6 — 25 beats.**

# 2. The build — the triad world as stills

Render every Act III settled state (25) into `review/act-3/states/` (`sheet.html` in beat order — the flipbook — and `states.json`), each cell carrying its provenance class:

- **The triad** (PORT, from `3-01`): drawn once in S11 and present as the act's home base per the kickoff — every scene's cells show it or return to it; its recede/return seams follow the Act II overlay grammar.
- **S12:** the triad splitting across goods; the household beat; **Argentina as the dated-fact grammar** landing on the split (ADAPT's one change); the principle statement.
- **S13:** the ladder restored — legacy `StageLadder` treatment with the stage marks as renders at lineup scale (collectible = `single_cowrie`; store, medium, unit per the manifest's assignments — flag any position lacking a natural render rather than guessing); **the attribution line on screen**; the social-technology and gate beats landing against the rising ladder; the foundation landing.
- **S14:** the coffee_cup render at display scale against the triad; the objection resolving to MEDIUM; the ladder-placement beat (the scene's payoff — bitcoin's position at stage two shown on the ladder just built); the pivot to STORE at full voice.
- **S15:** the tower (PORT, from `3-06`): the app → deposit → base-money descent, the wider-than-base proportions, the shiver, the fairness pair, the scoping, and the held question — ~~**the ClaimObject returns to the screen at the held question**, the thread resuming exactly there and nowhere earlier in the act.~~ **Struck 2 September 2026** (r2 ruling 1; trail: master §13): the clause was the kickoff's over-extension, restated here — the disc lives at the triad's center wherever the home base appears, and is not on the tower.
- Every station/stage arrival carries a line beneath it; composition law in full; no accent misuse; PORT cells are approved by provenance (`approved-port`), ADAPT cells `pending-review` with the one change named in the caption, in plain English.

**This is staging the approved kickoff, not open design.** No candidates anywhere (the map has no NEW frame). One honest render plus a plain-English flag for any genuine underdetermination.

# 3. Out of scope — hard

No implementation, no motion, no deck changes, no Act II or Act IV work, no new dark-field generation (flag gaps instead).

# 4. Verification (the complete list — nothing more)

Per-cell pipeline checks on all new cells; the script installation's zero-difference diff; `npm run build` clean; one boot smoke (39 slides, 0 errors) proving zero deck impact; commit granularity; clean tree; tag; report per AGENTS.md §16, mode named, **review protocol in plain English: the presenter walks the flipbook once — the question at every frame is "can I follow the act's argument by eye alone: three jobs, the split, the climb, the objection answered on the ladder, the tower, the held question?" — and returns per-cell verdicts on the pending-review set plus the go-ahead that unlocks implementation.**
