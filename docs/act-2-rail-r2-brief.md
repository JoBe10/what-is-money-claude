# Act II Rail — r2
## Six confirmed rulings executed: the rail fills left to right, the claim steps off, the vault folds in, the network grows out of the ledger

**Mode: FAST — lean scope, same discipline as the rail-states session:** checks not listed are not run.
**Model: Opus 5, max effort. Fresh CLI session.** (Ruled staging against the built rail system — no open design exists in this session.)
**Branch:** `film-rebuild`. **Prior tag:** `act-2-rail-states`. **Tag on completion:** `act-2-rail-r2`.
**First step:** commit this brief as `docs/act-2-rail-r2-brief.md`.
**Read before working:** `docs/act-2-staging-amendment.md`, the rail sheet and its builders (`review/act-2/rail/`), the rail-states report (the coinage PENDING flag), the installed S5 script (the shells virtue line is verbatim there), the band and register law entries in AGENTS.md.

---

# 1. The presenter's rulings (1 September 2026 — record first, granular commits)

1. **Fill order:** narrative order equals spatial order — **SHELLS leftmost**, then CATTLE, SALT, IRON, METALS, GOLD, COINAGE, CLAIM ON GOLD, LEDGER, BITCOIN. The rail fills strictly left to right at every beat; nothing ever lands left of an existing station. Zanzibar's featured moment returns to the far-left SHELLS station when it lands. (The rail is a competition record, not a chronology — the only dates on it are the featured facts; record this note with the ruling.)
2. **The shells caption:** an undefeated station is never blank. SHELLS arrives with its virtue beneath it, in the installed script's own words — **"Beautiful. Scarce. Hard to fake."** — replaced by the Zanzibar wound when it falls. Standing = virtue at station voice; fallen = wound at the dimmed-prior step. Record as the general rule: every station arrives with a line beneath it, always.
3. **The claim steps off the Act II rail.** The ClaimObject appears in no Act II rail beat; station illumination and the spoken narrative carry position. Consequence recorded with the ruling: the collapsed Gate 3's judgment object becomes **the rail's own continuity**, which the presenter's act viewing judges. (The claim's on-screen thread resumes where the architecture stages it in Act III and at Act IV's return.)
4. **The vault folds into the rail.** The standalone vault overlay (S7 b3's seam pair) is retired to file, render banked. Restaged: CLAIM ON GOLD arrives (the certificate render), a **thin dependency line arcs back to the GOLD station** — the service-path grammar — and *"A claim on gold in a vault. Trust required: the vault."* lands as the featured line at full voice. The dependency line persists in the record from that beat onward, exactly as the b4 note already stages it. S7 stays 5 beats; b3 and b4 restage per this ruling; the script is untouched.
5. **The band rule becomes equal visual weight:** one shared box with a height *and* width cap; wide renders (gold, metals, ledger_glow) scale to fit the box, never dominating tall subjects. One family, one weight, at focus and at recede. Re-measure and record the box in the band's law entry.
6. **The network restage:** the S9 b1 overlay no longer arrives as a detached diagram — **the mesh forms out of the LEDGER station itself**: the station's hub (the issuer the act just watched fail) dissolves, the mesh remains anchored where the ledger stood, the rail returns, and the coin takes its station beside it. The approved mesh geometry (`s9-b1-a`) is reused; only its origin and anchoring change. Palladium's placement is **confirmed as staged** — recorded to close the question.
7. **`coinage.png`** (presenter-generated, untracked): gate and ingest through the standard harness; it fills the flagged PENDING coinage station under the new band rule. On fail: file the evidence, flag, keep the stub.

# 2. The re-render

Re-render every rail-context state through the same builders with the rulings applied — the reorder, the captions rule, the claim removed, the band box, the folded vault beats, the restaged network seam, coinage's render — into `review/act-2/rail/` in place, `states.json` updated, the sheet re-cut in beat order. Overlay interiors (the periodic table, the mass state, the chart, palladium) are untouched and carry byte-identical; only their seam states inherit the reordered receded rail. Anything a ruling underdetermines: one honest render plus a plain-English flag, never a guess.

# 3. Out of scope — hard

No scene rewiring or deck changes (implementation is next, after the presenter's flipbook pass). No motion. No design candidates. No Act I or Act III work. No deck-wide verification.

# 4. Verification (the complete list — nothing more)

Gate evidence for coinage; per-cell pipeline checks on the re-rendered cells; byte-identity proof on the untouched overlay interiors; `npm run build` clean; one boot smoke (39 slides, 0 errors) proving zero deck impact; commit granularity; clean tree; tag; report per AGENTS.md §16, mode named, **review protocol in plain English: the presenter walks the re-cut sheet top to bottom once more — the rail should now fill left to right like history being written, every station arriving with a line beneath it, no white ball anywhere — and returns the go-ahead that sends Act II to implementation.**
