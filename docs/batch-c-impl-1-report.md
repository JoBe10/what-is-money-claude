# Batch C Implementation, Session 1 — Report
## The rulings recorded, the medium swapped, and Scenes 11–13 built — the disc holds the center as the act enters the engine

**Mode: FAST.** Scoped verification: the §1.3 re-renders through the states pipeline with a zero-pixel record proof over every untouched cell, the 15 built states proven against their approved cells three ways, the route smoked end to end both ways with reduced-motion parity, the production build, and the deck boot proven unchanged. No deck-wide suites — the deck was not touched, and the batch's own full verification is Session 2's.
**Branch:** `film-rebuild`. **Prior tag:** `act-3-states-r2`. **Tag:** `act-3-impl-1`.
**Date:** 2 September 2026.

---

# 1. The plain-English handoff — what stands, and what Session 2 does

**This session asks nothing of the presenter.** The three §1 rulings are recorded and executed, and by the shape of the batch the place his eye meets this work is **the act viewing after Session 2** — the deck run from Scene 11 at speaking pace, forward and backward, motion-on and reduced-motion, judging the act's own questions: does the triad feel like home base, does the ladder climb, does the coffee objection die when the coin lands, does the tower's shape argue, does the held question hold.

**What stands after this session:**

- **Scene 15 is candidate A** — the proportional inverted tower. The A cells are the approved S15 states Session 2 builds against; candidate B stays on file, one letter from return.
- **The go-ahead is recorded.** The whole r2 sheet is the approved set at 25 beats — the S12 brick glyph and the deliberate s14-b4 re-render seen and accepted by name, all fourteen flags closed as accepted records (`states.json` `approval` / `approvedSet`).
- **The medium mark is the hand-off, and the handshake never shipped.** The replacement passed the standard gate 5/5 (`grade-medium-drop.json`; shipping set re-proven 29/145/0), the retired handshake study is on file (`review/act-3/dark-field/medium--retired.png`), and the nine medium-bearing cells were re-rendered as the updated approved states **before any scene work**, so the scenes transcribe final geometry. The record proof pins it: **28 untouched byte-identical to the tag · 9 re-staged by the ruling · 1 carried · 0 failures.**
- **Scenes 11, 12 and 13 are built and proven** — every settled state its approved cell at **zero differing pixels, 15/15, three comparisons each** — and run as one continuous world at **`?proto=act3`**, behind the untouched 39-slide deck.

**Session 2 (Opus 5, max, FAST, fresh CLI):** build Scenes 14–15 against the same stage (the coffee study, the coin's protected landing, candidate A in motion with the transcribed shiver); splice all five into the manifest in place of the legacy slides the provenance map names (legacy files stay on disk; the section-3 waypoints and `3-04` flagged, never decided); run the batch verification across all 25 states, the full deck both ways, the eleven-question scene test; complete the capture strip; tag `batch-c`; report with the act-viewing protocol — and then the presenter watches the act, which is the gate.

---

# 2. The three rulings, recorded first, each its own commit

| # | Ruling | Where it is recorded | Commit |
|---|---|---|---|
| 1 | **Scene 15 is candidate A** — the proportional inverted tower; the A cells are the approved S15 states; candidate B retires to file (on disk, `on-file`, the aesthetic law's file-keeping clause) | master §13 · `docs/act-3-provenance.md` S15-F1/F2 (the reopened rows closed) · the sheet's meta and record | `936edf5` |
| 2 | **The go-ahead** — the r2 sheet approved in full: the brick glyph and the s14-b4 re-render accepted by name, the fourteen flags closed, the record recut as the approved set (25 approved · 6 on-file) | master §13 · `states.json` `approval`/`approvedSet` · the sheet recut as a record, not a review request · `check-cells.cjs` brought current (the selection gates — nothing widened) | `5d4038d` |
| 3 | **The medium render replaced** — the handshake read as agreement, not exchange; the hand-off gated and ingested through the standard harness (5/5, 0 held); the retired study on file; the framing row re-measured (`medium: [1.287, -1.5, 12.4]`, 842×474 — the family's aspect at a smaller frame, band boxes unchanged) | master §13 · `docs/dark-field-manifest.md` (the Batch C drop) · `src/dark-field.js` · the builders' subjects, alts and captions | `3e014e8` |
| — | **The nine medium cells re-rendered as the updated approved states** — s11-b3 · s11-b4 · s11-b5 · s12-b1 · s13-b5 · s13-b6 · s14-b2 · s14-b3 · s14-b4 — with the zero-pixel record proof over the whole sheet | `review/act-3/states/` · `proof-record-impl1.json` (**28 untouched · 9 re-staged · 1 carried · 0 failures**) · the pipeline checks **23/23 green** | `3df6117` |

**The nine-cell set is the complete medium-bearing set**, two cells beyond the brief's shorthand list ("the S11 spoke, the S13 stage, the S14 ladder cells"): `s12-b1` carries the receded home frame with the mark at 0.35, and `s14-b4` the returned home base — the non-negotiable is *the handshake never ships*, and those two cells carried it. Recorded here rather than decided silently.

# 3. The build — Scenes 11–13 as one continuous world

`src/scenes/act-3-the-jobs-of-money/` — `_jobsStage.js` (the shared stage), `_sceneModule.js` (the contract), and the three scene modules, registered at `?proto=act3` while they wait for the splice. **No manifest change; the deck is untouched at 39.**

**Every settled state is its approved cell, transcribed.** The geometry, the style strings (concatenation order included) and the component mounts come from `review/act-3/harness/states.mjs` — the builders that rendered the approved cells — with the stage root carrying the legacy section classes so the legacy stylesheet does the placing, and the shared rasterHint holding the rasterization contract. The landed-state proof closes it mechanically: **15/15 at zero differing pixels, three comparisons each.**

**The act's law, carried by construction:** the disc is built in exactly one place — the triad's hub, the legacy token by its own classes — and holds the center at full voice in S11, at the recede's 0.35 in S12 b1, never anywhere else; the neutral marks stand in the rails-law band box, each in its own aspect, arriving with the element that carries them; the foundation beat's accent is `StageLadder`'s own foundation state — the only orange in the three scenes.

**Ports move like they always did.** S11 and S12 animate as the legacy slides animate — dataset choreography with the legacy stylesheet carrying every duration (the token's 1100/1500ms arrival, the spokes' draw, the heads' and cells' rises with the legacy column stagger, the principle's landing). S13's climb is `StageLadder.applyState(state, { live: true })` at the states legacy `3-03` drives — the line draw, the stop reveals carrying the neutral marks up with them (the Act II band's proven arrival), the threshold schedule, the foundation bloom — with the order overlay's kicker, gate-line pair and `data-step="6"` settle as `3-03`'s own dataset flips.

**The new connective motion — where this session's judgment went, all of it flagged in §6:**

- **S11 → S12 (the morph):** the continuity line clears, the home frame recedes to the overlay grammar's 0.35 — the disc receding at its center — and the three heads land over it in their own reveal. One world; the idea persists.
- **S12's household → Argentina seam (b2 → b3):** a gesture the legacy never performed, composed from the legacy's own vocabulary — the standing row clears, and the incoming row plays `3-02` build 2's own arrival: cells in the legacy column stagger, kicker alongside.
- **S12 → S13 (the authored cut):** the question changes ("is there an order?" → the order), so the split world clears and the empty rising line enters with the attribution — `3-03`'s own build 1 over the shared stage.
- **The arrival lines** land beneath their stops in the rail's landing gesture (rise 10, 0.6s), the latest at full voice, priors settling to the dimmed-prior step; the b5 pair lands at the row register while the gate lines carry the beat.

Scripts installed as notes, verbatim from package §2 by mechanical injection (5 + 4 + 6 arrows; `review/batch-c/harness/install-scripts.cjs`, `--check` green), `[→]`-aligned with the first arrow spoken over the scene's entry. **S13's notes carry the Q&A armor block** after its six arrows — the armor is notes material by its own label (notes-only, never spoken, zero advances), and the installer takes it deliberately. Scene contract in full: cold entry gestures, morphIn per boundary, backward handoffs, interrupted-gesture completion, reduced-motion parity, teardown.

# 4. Files changed

## New
| file | what |
|---|---|
| `docs/batch-c-implementation-brief.md`, `docs/batch-c-impl-1-report.md` | the brief as given, and this report |
| `src/scenes/act-3-the-jobs-of-money/_jobsStage.js`, `_sceneModule.js` | the act's shared stage and contract |
| `src/scenes/act-3-the-jobs-of-money/11-…js`, `12-…js`, `13-…js` | Scenes 11–13 |
| `review/act-3/dark-field/medium--retired.png` | the retired handshake study, on file |
| `review/act-3/grade-medium-drop.json`, `grade-shipping-set-impl1.json` | the gate evidence (5/5; 29/145/0) |
| `review/act-3/harness/proof-record-impl1.cjs` + `proof-record-impl1.json` | the record's zero-pixel proof (28 · 9 · 1 · 0) |
| `review/batch-c/harness/…` + evidence | the script installer, the landed-state proof (15/15), the smoke, the capture strip (15 settled + 15 gestures) |

## Modified
| file | what |
|---|---|
| master §13 (three rows) | the selection, the go-ahead, the medium replacement — each with its trail |
| `docs/act-3-provenance.md` | the S15 rows closed by the selection |
| `docs/batch-c-package.md` | the pipeline line (the flipbook pass done) |
| `docs/dark-field-manifest.md` | the medium row amended; the Batch C drop recorded |
| `src/dark-field.js` | the re-measured medium framing row; the family note |
| `assets/dark-field/medium.png` | the hand-off, ingested over the retired handshake |
| `review/act-3/harness/states.mjs` · `capture-states.cjs` · `check-cells.cjs` | the approval meta, the record modes (`--record-only`, `--only`), the selection gates |
| `review/act-3/states/` (9 re-rendered + sheet + record) | the updated approved states |
| `src/proto/registry.js` | the three scenes at `?proto=act3` (leaves the list at the splice) |

**Out of scope, changed anyway: none.** No manifest change, no engine change, no legacy edit, no script change, no S14–S15 work beyond the ruled cell re-renders.

## Commits and tag
```
dbeb1b7  docs: the Batch C implementation brief is on file
936edf5  ruling(c.1): Scene 15 is candidate A
5d4038d  ruling(c.2): the go-ahead — the record is the approved set at 25 beats
3e014e8  ruling(c.3): the medium mark replaced — the hand-off ingested 5/5
3df6117  review(act-3): the nine medium cells re-rendered as the updated approved states
79cba57  scenes(act-3): the shared stage, and Scene 11
badfdbf  scenes(act-3): Scene 12
c894a69  scenes(act-3): Scene 13
b39a741  review(batch-c): the landed-state proof — 15/15 at zero pixels
17329e4  review(batch-c): the smoke — zero deck impact, parity 15/15
8f23c2d  review(batch-c): the capture strip started
```
Plus this report. **Tag: `act-3-impl-1`** at the report commit.

# 5. Validation

| check | result |
|---|---|
| **Medium gate (drop zone)** | **5/5 clauses, ingested, 0 held** — corners 1.16, border 1.46, 88.3% dark, R−B +162.7, warm 100% |
| **Shipping set post-ingest** | **29 images, 145 checks, 0 failures** |
| **Zero-pixel record proof** | 38 files: **28 untouched byte-identical to `act-3-states-r2` · 9 re-staged by ruling 3 · 1 carried · 0 failures** |
| **Pipeline checks** | **23/23** — the measured cells, map agreement with the amended provenance, the approved-set coverage, the selection gates, the thread, the neutral assignments, the climbers, the carried bytes, the retired files |
| **Landed-state proof (the scenes)** | **15/15 at zero differing pixels** — scene↔archived cell, fresh cell↔archived, scene↔fresh cell, every state |
| **Smoke** | deck boots unchanged (39 slides, first id, 0 errors) · route boots 3 scenes · **15-state traversal exact forward, monotonic backward** · all four boundary handoffs land the previous scene's end state · **15 cold entries settled** · **reduced-motion parity 15/15** by serialization · **0 console errors** |
| **Scripts** | `install-scripts.cjs --check`: **3/3 identical to the package**, 5 + 4 + 6 arrows |
| `npm run build` | clean |
| **Commit granularity / tree** | each ruling, each scene and each proof its own commit; the tree clean at every stop |

**Not run, and not claimed:** no deck-wide register/brightness/composition audit, no static-gate suite (nothing in the deck moved), no second-window notes check, no scene test (Session 2's, on all five scenes). FAST defers the global suites to Session 2's batch verification and the GATE.

# 6. Flagged, not improvised

1. **The medium set is nine cells, not the brief's shorthand seven.** `s12-b1` and `s14-b4` carry the mark through the home frame; "the handshake never ships" governs (§2's note). The r1-carried `s14-b1` carries no mark and stays byte-identical through three sessions.
2. **The compositing lesson the proof caught, recorded for Session 2:** a hidden stage layer left at `opacity: 0` still composites, and a composited layer beneath text flips its antialiasing — every settled absence is therefore `display: none`, matching the cells' absent-from-DOM truth. The proof failed 15/15 before this, and passes 15/15 after; the rule is now in the stage's comments at the site.
3. **The household → Argentina seam (S12 b2→b3) is composed, not transcribed** — the legacy never performed it (its columns only ever carried Argentina). It is built from the legacy's own arrival vocabulary and flagged for the act viewing; if it disappoints, it is one ruled iteration against the pinned states.
4. **The in-act boundaries are judgments from the paradigm rule**, the same way Batch B's were: S11→S12 a morph (the triad persists, splitting), S12→S13 an authored cut (the question changes). His eye rules at the viewing.
5. **S13's notes carry the Q&A armor block** after the six arrows (notes-only by its own label). A Session 2 verbatim gate should compare the arrow blocks plus S13's armor, and this is the recorded reason.
6. **The arrival lines' flight voices** ride element opacity during gestures and settle to the recorded color alphas at the snap (compositing-equivalent over the black stage); the settled states are the approved cells exactly.
7. **`check-cells.cjs` was brought current with the rulings, not widened**: the selection gates replaced the pending-selection gates, and beat coverage now reads the approved set. Both encode rulings.

# 7. Remaining judgment calls — all his, all at the viewing

- **The act viewing is the gate** (after Session 2): the triad as home base, the recede-and-split seam, the household → Argentina seam, the climb and the foundation's accent, then Session 2's coffee landing and tower.
- The two boundary judgments and the composed S12 seam (§6.3–6.4), should any read wrong in context.

# 8. Recommended next step

**Session 2** (Opus 5, max, FAST, fresh CLI): Scenes 14–15 against the same stage, the manifest splice, the batch verification, the completed strip, tag `batch-c` — then the presenter's viewing of the act, which closes the batch on his word.
