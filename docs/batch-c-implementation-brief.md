# Batch C Implementation — Act III Enters the Deck
## The triad world, the ladder, and the inverted tower — two sessions, then the act viewing that is the gate

**Mode: FAST, both sessions.**
**Model: Opus 5, max effort, both. Fresh CLI session each.**
**Branch:** `film-rebuild`. **Prior tag:** `act-3-states-r2`. **Tags:** `act-3-impl-1` after Session 1, `batch-c` after Session 2.
**First step (Session 1):** commit this brief as `docs/batch-c-implementation-brief.md`.
**Standing law applies in full.** The approved r2 sheet (post-§1 below) is the visual authority for every settled state; the installed scripts are the notes; the legacy builds (`3-01`, `3-02`, `3-03` + `StageLadder`, `3-06` + `LayerDiagram`) are the motion authority for every PORT — transcribed, not re-authored.
**Read before working (both sessions):** the r2 sheet and report, `docs/act-3-kickoff.md`, `docs/batch-c-package.md`, the legacy sources, the Batch B implementation reports (the transplant and splice discipline to follow).

---

# 1. The presenter's rulings (2 September 2026 — Session 1 records first, granular commits)

1. **Scene 15 is candidate A** — the proportional inverted tower: width is claim volume, solidity is realness, the reveal descends from PAYMENT APPS to the narrow near-luminous base, the shiver at b4. Candidate B retires to file. The A cells are the approved S15 states.
2. **The go-ahead is given** — the r2 sheet approved in full, including the S12 saved column's legacy brick glyph (presenter-seen, accepted as part of the exact revert) and the deliberate s14-b4 re-render.
3. **The `medium` render is replaced (presenter-ruled):** the handshake read as agreement, not exchange; the new presenter-generated `medium.png` (the mid-transfer hand-off) supersedes it. Gate and ingest through the standard harness; the prior render retires to file; re-render the cells that carry the medium mark (the S11 spoke, the S13 stage, the S14 ladder cells) through the states pipeline as the updated approved states — this is Session 1's first build step, before any scene work, so implementation transcribes final geometry. On a gate fail: file evidence, flag, and stop the affected cells at pending (do not ship the handshake).

# 2. Session 1 — Scenes 11, 12, 13 built (tag `act-3-impl-1`)

Build the scene modules against the approved cells:

- **The triad home base** (S11, PORT): the disc at center, the three job objects at the spokes, motion transcribed from `3-01`'s builds; the S11 b5 retrospective landing.
- **S12** (PORT staging): the columns, the household fill, the Argentina row arriving as `3-02` performs it, the principle landing.
- **S13**: the rising-line ladder per the r2 cells — the attribution on stage from beat 1, the neutral marks arriving at the band scale with their lines beneath, the social-technology and gate beats landing in the clear sky, the foundation beat taking the act's accent exactly as the approved cell has it. Ladder motion transcribed from `3-03`/`StageLadder` where it exists; stage-mark arrivals follow the Act II band's proven arrival gesture.
- Every settled state its approved cell at zero pixels; scripts as notes, `[→]`-aligned; scene contract in full; no manifest changes this session.
- Verification: landed-state proofs (15/15); per-scene smoke; build clean; boot smoke; capture strip started in `review/batch-c/`; commit granularity; tag; plain-English handoff.

# 3. Session 2 — Scenes 14, 15, the splice, the batch close (tag `batch-c`)

- **S14**: the coffee study against the condensed home row; the objection resolving on the returned ladder; the coin's landing at stage two (the scene's payoff — protect its timing: the coin arrives alone, the only monetary object on the ladder); the pivot to the whole triad with STORE at full voice.
- **S15, candidate A in motion**: the descending reveal — each layer landing beneath the last, width and solidity per the approved cells; the shiver transcribed from `3-06`'s proven choreography over the A geometry; the fairness pair, the scoping recede, and the held question over the composition as approved (no disc).
- **The manifest splice**: Scenes 11–15 in place of the legacy slides they supersede per the provenance map's sources (`3-01`, `3-02`, `3-03`, `3-06`, `3-07`, with ambiguous fates — the section-3 waypoints, `3-04` — flagged, never decided; legacy files on disk). Old and new slide counts stated. Boundaries seamless both directions: S10 → S11, S15 → the surviving legacy deck.
- **Batch verification**: landed-state proofs across all 25 states; full deck traversal both ways; cold entry at every new build; reduced-motion parity; notes and self-reference scans; the eleven-question scene test on all five scenes, honest verdicts, tensions flagged not fixed; capture strip completed; commit granularity; clean tree; tag; report per AGENTS.md §16, mode named.

# 4. Out of scope (both sessions)

No design (underdetermination is a stop-and-flag), no Act II or Act IV work, no engine feature changes, no script changes, no legacy deletion.

# 5. The presenter's review (state it in Session 2's report, plain English)

**The act viewing is the gate.** Run the deck from Scene 11 at speaking pace, forward and backward, motion-on and reduced-motion. The act's own questions: does the triad feel like home base; does the ladder climb; does the coffee objection die on screen the moment the coin lands at stage two; does the tower's shape argue before a word is spoken; does the held question hold. The word that closes the batch is the presenter's.
