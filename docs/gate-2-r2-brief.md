# Gate 2 — Motion r2
## The nineteen approved states, in motion — derivation banned, character is the craft

**Mode: FAST.** Prototype work; scoped verification.
**Model: Fable 5, top effort. Fresh CLI session.**
**Branch:** `film-rebuild`. **Prior tag:** `gate-2-states`. **Tag on completion:** `gate-2-r2`.
**First step:** commit this brief as `docs/gate-2-r2-brief.md` if not already present.
**Standing law applies in full, including the full-coverage rule** (docs/gate-2-states-brief.md header): every settled state in this prototype must be an approved cell of the beat-state sheet — no derived states, anywhere, for any reason. If a needed state has no approved cell, stop and flag.
**Read before working:** `docs/gate-2-states-brief.md`, `docs/gate-2-states-report.md`, the approved cells (per §1 below) at full size, `docs/gate-2-r1-report.md` (r1's motion inventory — what exists to adapt), `docs/gate-2-brief.md` §3 and §6 (the scene meanings and the bar — still binding).

---

# 1. The presenter's per-cell rulings (27 August 2026 — the approved state set)

Record these in `review/gate-2/states/states.json` (per-cell status) and in a dated selections record appended to `docs/gate-2-states-report.md`. Non-selected candidates stay on file.

| Beat | Approved state |
|---|---|
| S2 b1 | `s2-b1-a` (carried composition; capabilities accumulate here per R2) |
| S2 b2 | `s2-b2` |
| S2 b3 | `s2-b3-a` — patient dimmed in place (D1-A); goods clustered at the surgeon's side (D2-A) |
| S2 b4 | `s2-b4-c` — **the path-failure language is the thinning stroke (D3-C), film-wide** |
| S2 b5 | `s2-b5-b` — the two people as the statement's anchor (D4-B) |
| S3 b1 | `s3-b1` (carried birth frame) with remnant language C (`s3-b1-c`'s remnant) |
| S3 b2 | `s3-b2` |
| S3 b3 | `s3-b3` |
| S3 b4–b6 | `s3-b4-a`, `s3-b5-a`, `s3-b6-a` — interval typography system A |
| S3 b7 | `s3-b7-b` — completion as two phases in one frame (D5-B) |
| S3 b8 | `s3-b8-a` |
| S3 b9 | `s3-b9-a` — full handoff to clean black under the separation lines (D4-A) |
| S4 b1–b5 | `s4-b1-b` … `s4-b5-b` — **Scene 4 is system B throughout** |

All single-candidate cells: approved as rendered. The prior standing rulings hold: birth default **pool** (condense banked), micro-motions kept for live judgment, S3→S4 **morph**, no accent before the birth, patient photographic everywhere.

**One new component-level ruling:** in the ClaimMark's **carrier context**, the disc sits **centered between the carrier arcs, fully symmetric** (presenter markup, 27 Aug, from the legacy 4.05 reference). Fix in the component; it must hold at every scale of the Gate 1 sheet's carrier row. No current Gate 2 state uses the carrier — this ruling protects Act II.

# 2. The build

Re-implement the Gate 2 prototype to the ruled structure: **19 beats** (S2 = 5, S3 = 9, S4 = 5), one advance each, verbatim scripts from the amended `docs/batch-a-package.md` §2 as the notes.

- **Geometry is transcribed, not re-derived:** take every settled state's geometry from the beat-state harness (`review/gate-2/states/` builders) so all 19 landed states match the approved cells **by construction**, then prove it mechanically (§5). Adapt r1's motion where it serves; discard where it doesn't — r1's structure (three linked modules, the engine's continuity groups, the shared stage) stands.
- **What moves is the craft.** Between approved states, motion must be argument (gate-2-brief §6). Specifically:
  1. **The S2 entry (D8's motion half).** The presenter is concerned the opening reads somber. Author the entry with purpose and warmth: light rises on the scene, the service path draws itself, the capabilities land line by line under his voice. The settled state is `s2-b1-a` exactly; the way in is this session's craft, judged live.
  2. **The failing path in language C:** the return path's stroke visibly thins and dies before arriving — drawn with intent, failing with meaning, in both lunges of S2 b3→b4. Its S3 b1 remnant is consumed by the birth (pool).
  3. **The completion (S3 b7, D5-B):** stage the two phases so the claim *departs* and the shoe *arrives* as distinct overlapping events — at no point may the claim read as transforming into the shoe. Settled state is `s3-b7-b` exactly.
  4. **The interval (S3 b4–b6):** each name lands at full voice on its own advance and demotes its predecessor, per system A's cells.
  5. **Statements:** S2 b5 anchors on the two people (B); S3 b9 completes a full recorded handoff to clean black (A) before the lines land.
  6. **Scene 4 in system B's grammar** across all five beats; the S3→S4 morph keeps the claim unbroken while the world generalizes.
- Reduced-motion parity, direct entry at all 19 builds, backward traversal, full cleanup — the scene contract, as in r1.

# 3. Explicitly unchanged

Birth treatments all remain runtime-selectable (default pool). The notes overlay, ports discipline (never 5173), zero deck/engine/manifest impact — all as r1.

# 4. Out of scope

No new compositions, no new cells, no dark-field work, no deck scenes, no engine changes. Script wording untouched beyond what the states session already amended. Any gap: flag, never improvise.

# 5. Verification (FAST scope)

- **Landed-state proof:** all 19 settled states mechanically compared against the approved cells' renders (same pipeline, same geometry source) — this is the full-coverage rule made checkable. Any mismatch is a defect, not a note.
- The r1 suite at 19 beats: forward/backward traversal, cold direct entry at every build, reduced-motion parity byte-compared, 0 console errors, `npm run build` clean, deck boot smoke (45 slides, 0 errors).
- Capture strip → `review/gate-2/r2/`: 19 settled beats + the pool birth mid-gesture + the S2 entry mid-gesture + the S3 b7 two-phase mid-gesture.
- Commit granularity; clean tree; `gate-2-r2` tagged; report per AGENTS.md §16, mode named.

# 6. The review that follows (state it in the report)

The presenter reviews r2 **live, as a viewer** — `?proto=gate2`, beat by beat, forward and backward, motion-on and reduced-motion — judging: the S2 entry's temperature, the pool birth in context, the micro-motions (keep or cut), the S3→S4 morph, and the whole against the §6 bar of the original gate brief. His notes drive r3 if needed. **The gate closes only on his recorded word: exceptional.**
